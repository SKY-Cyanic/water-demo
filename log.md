# Development Log

## 기록 규칙

- 최신 항목을 위에 추가한다.
- 추측과 측정 결과를 구분한다.
- 실패를 삭제하지 말고 해결 상태를 이어서 기록한다.
- “수정함”만 쓰지 말고 재현 조건과 검증 결과를 남긴다.
- 콘솔 오류는 핵심 메시지와 최초 원인을 기록하되 거대한 로그 전체를 붙이지 않는다.

## Final QA

- Commit/build: git 미사용. `index.html` + `src/**` (빌드 단계 없음), 2026-07-31
- Browser: **HeadlessChrome 151.0.0.0** (CDP 제어, 실제 rAF 구동). 인앱 브라우저는 검증에 사용하지 않음
- OS/GPU: macOS 26.5.2 · Apple M5 10-core
- Viewport: 1920×1080 (주), 1280×720, 3840×2160
- Backend: **WebGPU** (`?forcewebgl=1`에서 WebGL 2 정상 렌더 확인)
- Quality: High (기본), Low/Ultra 측정 포함
- Average FPS / frame time: **50.9 fps / 19.64 ms** (1920×1080 High, 12초 샘플)
- 1% low: **29.9 fps** — vsync 양자화(33.3 ms = 2 간격)이지 히치가 아니다.
  16.67 ms를 조금 넘기면 프레임이 33.3 ms 칸에 떨어진다. 사용자가 승인한 하한 45 fps 위.
- Console errors: **0**
- 프리셋: **10종** (Calm Lagoon / Open Sea / Golden Hour / Storm Front / Moonlit /
  Trade Winds / Coral Shallows / Arctic / Dusk / Sea Fog)
- Screenshot paths: `qa/qa-*.png` 14종
- Reports: `qa/report-shots.json`, `report-perf.json`, `report-stress.json`, `report-fft.json`

---

## 2026-08-01 — Phase 17: 항적 시계 분리 · 수중 광선

**문제 35 — 배 뒤 흰 선이 계속 남아 있었다 (내가 두 번 "고쳤다"고 잘못 보고함)**

- 증상: 사용자가 세 번 보고. 배 뒤에 흰색 선들이 계속 따라다닌다.
- 앞선 두 번의 시도가 왜 실패했는지: 둘 다 **모양** 문제로 보고 손댔다.
  한 번은 감쇠 길이, 한 번은 선체 프레임 → 월드 공간 이동. 원인은 모양이 아니었다.
- 진짜 원인: 두 버전 모두 포말 히스토리의 **같은 채널(.r)** 에 쓰고 있어서
  백파의 감쇠를 그대로 상속했다. 그 수명은 `0.2 + persistence² × 42` —
  Open Sea 21초, Storm Front 34초다. **깨지는 마루가 남긴 거품 뗏목**에 맞게 쓴 값이고
  거기에는 맞다. 1.55 m/s에서 이건 32~53 m의 꼬리다. 게다가 침착 진폭이 클램프 전 1.35라
  꼬리가 순백에서 시작해서 포말 임계 아래로 떨어지기까지 한참 걸렸다. 그게 흰 선이다.
- 튜닝 실수가 아니다. **선체의 교란과 깨지는 마루는 다른 과정인데 둘 사이에 시간 상수가
  하나뿐이었다.** 히스토리 타깃은 RGBA이고 `.r`만 쓰고 있었으므로 항적을 `.g`에
  고정 8초 시계로 옮겼다 — 혼입된 공기는 해상 상태와 무관하게 10초 안에 떠올라 터진다.
  표면은 `max(r, g)`를 읽는다. 같은 텍스처, 같은 패스, 추가 비용 0.
- 칠하는 창은 26 m → 14 m로 당기고 침착값을 0.58/0.34로 낮췄다. 보이는 길이가
  이제 **속도 × 수명**에서 나온다.
- 부수: 표면 셰이더가 켈빈 도형 전체(팔·중앙 트레일·나이 감쇠)를 계산해놓고
  버리고 있었다. 배 근처 픽셀마다 죽은 ALU이고, 더 나쁘게는 **표면 셰이더가 아직
  항적을 담당하는 것처럼 읽혀서** 나를 두 번이나 틀린 파일로 보냈다. 삭제.

**문제 36 — 인앱 브라우저에서 시뮬레이션이 얼어 있다 (또 계측 오진)**

- 브라우저 창에서 `vessel.update()` 호출 수를 세었더니 0, `_t`도 0.
  "update가 아예 안 불린다"고 진단했는데, `env.u.time.value`가 0에 고정이었다.
  **탭 전체가 얼어 있었다.** `research.md` §8에 이미 적혀 있는 그 함정이다.
- 움직임에 관한 모든 질문은 `tools/qa.mjs`(실 Chrome CDP)로만 판단할 것.

**문제 37 — 수중 뷰: 검은 선체 · 광선 없음**

- 선체가 수면 아래에서 **평평한 검은 실루엣**이었다. 원인: 방오도료는 매우 어두운
  페인트인데 거기에 물 색을 곱하니 형태가 전부 뭉개졌다. 수중에는 앞의 물기둥보다
  어두운 표면이 존재할 수 없다 — 사이의 물이 빛을 산란해 넣고, 그게 알베도가
  내려갈 수 없는 바닥을 만든다. 바닥값을 넣고, **선체에 카우스틱을 넣었다.**
  물체가 물 *안에* 있다고 말하는 단 하나의 단서다.
- 광선(god ray)을 넣었다. 장식용 줄무늬가 아니라 **수면의 카우스틱을 태양 방향으로
  압출한 것**이므로 레이마치한다.

**문제 38 — 광선 마치가 세 번 연속으로 틀렸다**

1. **스텝을 `dist`에 비례**시켰다 → 화면 전체가 카메라 축으로 수렴하는 별폭발.
   수면까지의 거리는 아래에서 보면 그 자체가 **화면 방사 함수**라, 모든 샘플이
   방사적으로 스케일된 좌표에 떨어진다. 스텝이 화면 방사량에 비례하는 마치는
   전부 이 실패를 갖는다. → 고정 거리 마치로.
2. **`causticPattern`을 공유**했다. 셀이 0.87 m인데 10스텝×55 m는 5.5 m 간격 —
   여섯 셀 건너뛴다. 연속 샘플이 무상관이라 마치가 잡음을 적분했고 직조 모아레가 나왔다.
   → 광선 전용의 굵은 능선형 fBm. 광선은 **폭이 넓은 기둥**이고, 가는 웹은 그것을
   해상할 픽셀이 있는 표면에 속한다.
3. **`max(p.y.negate(), 0)`**. 수면 위로 나간 샘플이 깊이 0으로 클램프되어
   **최대 기여**를 했다. 광선이 y=0을 지나는 자취가 주름이 되었고, 그 자취가
   카메라 축을 지나므로 모든 수중 샷 한가운데에 접힘이 그려졌다.
   **값을 클램프하는 것과 샘플을 배제하는 것은 다르다.**

**문제 39 — 성능: 네 번째 열 드리프트**

- 14스텝 × Worley 2회 = 픽셀당 28 Worley. 수중 **14.7 fps**.
- 싼 필드로 교체 후 38.6. 스텝을 8 → 6으로 **줄였더니 31.9**가 나왔다.
  같은 방향 변경에 반대 결과 — 기계가 데워진 것이다.
- 더 추측하지 않고 **`?noshafts=1` 킬 스위치**를 넣었다. `research.md` §9가
  요구하는 바로 그것인데, 이번에도 만들기 전에 두 번 추측했다.
- 최종 수치는 연속 QA로 데워진 상태에서 잰 것이다: 1080p High 50 fps,
  수중 29.2. 식은 상태에서는 각각 60과 38.6이 나왔다.
  **차가운 기계에서 `?noshafts=1` A/B로 재측정해야 정확한 귀속이 된다.**

---

## 2026-08-01 — Phase 16: 물체(body) 조명 · 선체 그림자 분리 · 카우스틱 반전 수정

**목표**

사용자 지시: 참조 데모 수준을 따라잡을 것. 이번에는 질문 없이 진행.
QA 스크린샷을 실제로 들여다보고 결함부터 잡았다.

**문제 30 — 물의 부피 색이 파면 방향과 무관했다 (이번 회차 최대 결함)**

- 증상: 전경 물이 형태는 있는데 음영이 없다. 파도가 "움직이는 평면"으로 읽힌다.
  사용자가 상용 참조와 비교하며 처음 지적한 항목이 바로 이것이었고,
  Phase 15-3에서 흡수 계수를 살려낸 뒤에도 남아 있었다.
- 원인: `volume`을 만드는 조명 항이 **`ambient`와 `skyLight` 두 상수뿐**이었다.
  둘 다 법선과 무관하다. 그런데 아래를 내려다보는 시선에서는 Fresnel이 반사를
  2% 근처로 눌러버리므로, 전경 픽셀의 색은 사실상 **전부** 이 상수 조합이다.
  즉 파면이 어느 쪽을 향하든 같은 색이었다. 형태만 있고 셰이딩이 없었던 것.
- 수정: 햇빛이 굴절해 들어가 산란되어 나오는 성분을 추가.
  `bodyLight = ambient * mix(1, nDotL + 0.5, bodySunGain)` —
  `nDotL = 0.5`에서 평균 보존이므로 10개 프리셋의 노출은 그대로고 대비만 재분배된다.
  `bodySunGain = 0`이 기존 화면을 정확히 재현한다.
- 단, `nDotL`은 **파도 스케일 법선 `Nw`**로 계산한다. 굴절·산란되어 나온 빛은
  픽셀 안의 경사 분포 전체로 평균된 것이라 잔물결을 따라가지 않는다.
  픽셀 단위 법선을 쓰면 스페큘러와 같은 앨리어싱이 물체 색에도 생긴다.
- 재검증: `qa-open-sea` / `qa-trade-winds`에서 파면별 명암이 생겼다.

**문제 31 — 배를 따라다니는 검은 얼룩**

- 증상: 컨택트 시트에서 선체 옆에 크고 어두운 슬래브가 붙어 다닌다. 지난 회차에
  발견하고 "선체 그림자인지 새 깊은물 볼륨의 파도 골인지 구분 못 했다"고 남겨둔 것.
- 원인 (둘이었다):
  1. 그림자가 `oneMinus(smoothstep(0.72, 1.15, d)) * 0.34`를 `body`에 곱하는
     **단일 항**이었다. 태양 차폐와 하늘 차폐는 성질이 전혀 다른데 하나로 뭉쳐 있었다.
     이미 어두운 골에서 34%를 더 깎으면 검정이 된다. 테두리도 단단했다.
  2. `?noreflect=1`로 확인한 결과 나머지 절반은 **선체 자신의 반사**였다.
     물리적으로는 맞지만 너무 선명하게 유지되고 있었다.
- 수정:
  - 태양 차폐 `sunOcc`: 선명한 오프셋 그림자. **스페큘러만 죽인다.** 물 위에서
    그늘로 읽히는 것은 색이 어두워지는 것이 아니라 하이라이트가 사라지는 쪽이다.
    태양 고도가 낮을수록 반음영을 넓힌다.
  - 하늘 차폐 `skyAO`: 선체 바로 아래에만, 부드럽게, 20%. 하늘은 반구광이고
    선체는 자기가 덮은 입체각만 가린다. 게다가 수면 아래에서 나오는 빛은
    애초에 그 입체각을 본 적이 없다.
  - 프롭 반사는 `rough`에 자리를 내준다. 거울상은 그것을 담을 만큼 매끈한 물에서만
    살아남는다. 표면 경사에 따른 흔들림도 2배로 늘렸다.

**문제 32 — 카우스틱이 셀 중심에 그려지고 있었다**

- 증상: Coral Shallows / Calm Shallows 바닥 전체가 육각 격자로 배열된 동그란
  덩어리. 뽁뽁이처럼 보인다.
- 원인: `1 - min(w1, w2)`. Worley의 F1 거리는 **셀 중심에서 0, 경계에서 최대**다.
  뒤집으면 중심에서 peak가 되고, 7제곱하면 중심만 남는다. 바로 위 주석은
  "가는 필라멘트"라고 적혀 있었다 — 코드가 주석의 정반대를 하고 있었다.
- 수정: 파면이 접혀 빛이 모이는 곳은 **곡선**이다. 두 필드가 **일치하는 곳**에
  웹을 그린다. 지수함수를 써서 선 폭을 직접 지정한다(제곱 지수는 어느 한쪽
  스케일이 움직일 때마다 다시 추측해야 한다). 셀 크기도 2.4 m → 0.87 m.
- 부수: 리프가 얼룩으로 보이던 것은 바닥의 유일한 텍스처가 등방성 노이즈였기 때문이다.
  몇 미터의 물기둥은 그것을 저역통과시켜 없애버린다. **모래 물결**을 넣었다 —
  방향성이 있는 무늬는 텍스처보다 훨씬 낮은 대비로도 눈이 재구성한다.

**문제 33 — 밤에 포말이 광원처럼 빛났다**

- 증상: Moonlit에서 항적이 거의 검은 바다 위에 불투명한 흰 리본으로 떠 있다.
- 원인: `foamLit = foamColor * (ambient * 0.62 + …)`. `ambient`에는 파도 골이
  검게 죽지 않게 하려고 넣은 **0.30 하한**이 있다. 그 하한이 밤에도 포말을 밝혔다.
- 수정: 포말은 확산 백색 반사체다. 색은 곧 도달하는 빛이다.
  `skyLight * 1.15 + sunColor * nDotL * sunIntensity * 0.55`로 교체.
  하늘 항은 하한과 달리 해가 지면 같이 떨어진다.

**문제 34 — 다시 측정기에 속았다 (세 번째)**

- Storm Front의 spark가 4.59% → 10.9%로 보여 회귀로 판단하고 두 번 추측 수정했다.
  둘 다 효과 없음(10.9, 11.0).
- 같은 빌드(HEAD~2)를 **지금 이 기계 상태에서** 다시 재어보니 **10.78%**였다.
  회귀는 없었다. 커밋된 리포트의 4.59%가 다른 조건에서 나온 값이다.
- 교훈 갱신: 이미 `research.md` §9에 "행동하기 전에 다시 재라"라고 적어두고도
  **커밋된 리포트를 기준선으로 신뢰**했다. 기준선은 파일이 아니라 **지금 돌린 측정**이다.
  플리커 리포트에 측정 시각과 함께 "동일 세션 재측정 없이 비교 금지"를 남긴다.
- 추측 수정 두 건은 그 자체로는 옳아서 남겼다(물체 조명의 `Nw`, 거품 디테일 거리 페이드).

**성능**

1920×1080 High 50.9 fps (19.64 ms), Storm 53.2, 수중 49.1, 720p 60.
1% low 29.9는 vsync 양자화다. 사용자가 승인한 하한 45 fps 위.
구름 타깃 해상도는 0.4 유지 — 0.52는 마치 비용 1.7배에 비해 얻는 것이 적었고,
선명해진 것은 해상도가 아니라 디테일 옥타브 진폭이었다(그쪽은 공짜다).

---

## 2026-07-31 — Phase 14: 하늘 디더링 제거 · 얕은 물 재작업 · 프리셋 10종 · 비

**목표**

사용자 지시 두 가지. (1) 상용 참조 데모(threejswaterpro.com) 수준에 최소한 근접할 것,
시각 품질이 항상 최우선. (2) 참조가 가진 프리셋 범주를 전부 포함할 것.

참조는 일반 사용자로서 열람만 하고 색·구도·기능 범주만 기록했다. 코드·셰이더·자산·설정값은
추출하지 않았고, 브랜드명과 게임 타이틀명(Black Flag, Sea of Thieves)은 사용하지 않고
동등한 환경 범주를 자체 명명으로 구현했다. `research.md` §4 경계 그대로다.

**문제 17 — 하늘 전체에 격자 무늬 (최우선 시각 결함)**

- 증상: 모든 프리셋에서 구름 픽셀에 규칙적인 직조/방충망 패턴. 1:1에서 명확히 보임.
  이전 문서에는 "Moonlit 하늘에 옅은 지터 디더링"이라고 적어두었으나, 실제로는
  Open Sea와 Golden Hour에서 훨씬 심했다. 심각도를 잘못 기록했던 것.
- 원인 (둘이 겹침):
  1. 지터 해시가 `fract(sin(dot(screenUV, k)) * 43758)`. screenUV를 넣으면 sin의 인자가
     ~4e6까지 커지는데 float32의 절대 정밀도는 그 크기에서 약 0.25다. 난수열이 격자로
     붕괴한다.
  2. 정밀도를 고쳐도 백색잡음은 1 샘플/픽셀 레이마치의 디더로 부적합하다. 눈이 입자로
     분해한다. 진짜 원인은 스텝당 광학 두께가 커서 지터 진폭이 그대로 밝기 차가 되는 것.
- 수정: 해시를 **픽셀 좌표 기반 interleaved gradient noise**(Jimenez)로 교체 —
  3×3 이웃에서 저불일치라 인접 픽셀이 상보적 오프셋을 잡고 스텝 구조가 공간적으로 상쇄된다.
  동시에 스텝 수를 High 7 → 24로 올리고 옥타브를 3 → 5로 올렸다.
- 재검증: `qa/qa-open-sea.png` 3.5배 확대에서 패턴 소멸, 적운 형태가 오히려 선명해짐.

**문제 18 — 구름 품질을 올리자 프레임 타임이 2배**

- 증상: 1920×1080 High가 16.67 ms → **31.25 ms**.
- 원인: 마치가 전 화면 해상도에서 돌고 있었다. 스텝×옥타브가 21 → 90 옥타브/픽셀.
- 수정 3단계:
  1. **밀도 분리**: 기저 2옥타브 + 침식 3옥타브. 침식의 기여가 `DETAIL_GAIN`으로
     유계이므로 임계값에서 그만큼 아래인 샘플은 *정확히* 건너뛸 수 있다.
     처음 잡은 0.34는 fBm 표준편차의 2배라 거의 모든 샘플이 비싼 분기를 탔다 —
     최적화가 잡음으로 측정됐다. 0.16으로 낮춰 절반이 걸리게 했다.
  2. **전용 저해상도 패스** (`src/env/clouds.js`): 화면의 0.4배 RT에 돔을 한 번 그리고
     하늘 돔은 `screenUV`로 합성만 한다. 두 패스가 같은 카메라로 같은 구를 래스터화하므로
     투영 수식이 필요 없고 백엔드 규약에 의존하지 않는다.
  3. 슬랩을 1500–4200 m → 1400–3100 m로 좁혀 같은 스텝 수에서 dt를 줄였다.
- 재검증: 1920×1080 High **16.67 ms**, Storm도 **16.67 ms**(이전 17.14),
  3840×2160 Ultra **52.64 ms**(이전 61.29). 품질을 올리고 비용은 내려갔다.

**문제 19 — 저해상도 RT에서 하늘이 검게 나옴**

- 증상: 구름 없는 하늘 영역이 완전히 검정.
- 원인: cover를 알파에 담았는데 `NodeMaterial`은 불투명 재질의 알파를 1로 강제한다.
  합성식이 `sky*(1-a) + rgb`이므로 모든 픽셀이 "빛을 산란하지 않는 구름에 완전히 가려짐"이
  되었다.
- 수정: RT 재질을 `transparent = true` + `blending = NoBlending`. 채널은 보존하되
  합성은 하지 않는다.

**문제 20 — 0.4배 RT를 확대하니 지터가 체크무늬로 보임**

- 원인: 지터 패턴은 정확히 RT의 픽셀 주파수에 있다. 2.5배 확대하면 2.5 px 무늬가 된다.
- 수정: 합성 시 **반 텍셀 회전 격자 4탭**. 1/6 해상도 텍스처에 대한 3회 추가 페치로,
  bilinear 업샘플이 담아내지 못하는 대역을 걷어낸다.

**문제 21 — Calm Lagoon이 파스텔 우유 (S1 시각 결함)**

- 증상: 프레임 전체가 연두빛 유백색. 수면도 바닥도 깊이감도 없음.
  이전 문서에는 "카우스틱이 다소 과노출"로 적혀 있었으나 실제로는 프리셋이 망가진 상태였다.
- 원인 넷:
  1. 카우스틱 게인이 무제한. `causticPattern`은 최대 ~2.8이고 여기에 강도 1.25를 곱한 뒤
     0.85로 바닥색을 곱하고 있었다 → 승수 ~3.7.
  2. 카우스틱이 수심과 무관했다. 실제로는 가시거리를 제한하는 그 산란이 집속도 파괴한다.
  3. **바닥이 평면**이었다. 수심이 한 값이면 물기둥 두께도 한 값이고 석호 전체가 한 색이 된다.
  4. 모래 알베도가 1에 가깝게 저작되어 있었다. 젖은 모래는 1/3 정도만 반사한다.
     밝은 바닥이 그 위의 물을 이겨서 물색이 남지 않았다.
- 수정:
  - `shading.js`에 `seabedHeight` / `seabedAlbedo`를 두고 **수면의 해석적 굴절과
    해저 메시가 같은 함수를 공유**하도록 했다. 두 렌더링이 어긋나면 수면을 넘을 때 바닥이 튄다.
  - 지형은 300 m / 45 m / 8 m 세 스케일. 눈에 보이는 일을 하는 것은 45 m 대역이다.
    3 m 높이에서 보이는 바닥은 40 m 남짓이라 300 m 기복은 상수이고 8 m 기복은 질감이다.
  - 굴절 교점을 2단계로 푼다(공칭 평면 → 그 지점의 지형으로 재해).
  - 카우스틱에 `exp(-수심·0.11)`과 클램프.
  - 알베도 기저를 0.30 + grain·0.22로 낮추고, 잡초/암초 패치의 대비를 크게 키웠다.
  - `depthNorm`을 가시거리가 아니라 **바닥이 있으면 24 m** 기준으로 정규화. 이전에는
    7 m 석호가 그라디언트의 0.15에 놓여 전체가 같은 얕은 색이었다.
  - 산호모래를 `#e9ece4`로 흰색에 가깝게. 황갈색 바닥은 투과광에서 청색을 빼앗아
    석호를 청록이 아니라 초록으로 만든다.
- 재검증: `qa/qa-calm-lagoon.png`, `qa/qa-coral-shallows.png`. 청록 + 바닥 질감 + 깊이 변화.
  참조의 결정질 얕은 물에는 아직 못 미친다(아래 남은 격차).

**문제 22 — 얕은 물·비 작업이 조용히 전 프리셋의 프레임을 먹었다**

- 증상: 1920×1080 High 16.67 → **20.55 ms**. 바닥이 없는 프리셋에서도.
- 원인: 해석적 해저가 픽셀당 약 14 옥타브인데, 10개 중 6개 프리셋은 바닥이 아예 없다.
  비 오버레이도 마른 프리셋에서 그대로 돌고 있었다.
- 수정: 둘 다 유니폼 조건 분기로 감쌌다(`seabedMix`, `rainAmount`). 유니폼이므로
  드로우 전체에서 분기가 일관되어 꺼져 있을 때 사실상 공짜다. 픽셀 데이터로 나누는
  분기였다면 성립하지 않는다.
- 재검증: 전 조건 1920×1080 **16.67 ms** 복귀.

**추가 — 프리셋 5종과 비**

- 신규: Trade Winds(진한 열대 청색) / Coral Shallows(수심 3.6 m 산호초) /
  Arctic(저각 태양 + 흐림) / Dusk(블루 아워) / Sea Fog(짙은 안개 + 비).
  총 10종, 단축키 `1`–`9`와 `0`.
- 비는 화면 공간 2층. 격자 자체를 (시간이 실린) y로 x 방향 전단해서 기울인다.
  y에 x를 더하는 쪽이 먼저 떠오르지만 그것은 어느 셀에 물방울이 놓이는지만 뒤섞고
  줄기는 수직 그대로다. `task.md` P1 "폭풍 비/스프레이" 항목이 닫혔다.

**측정 (12초 샘플, autoQuality off)**

| 조건 | 평균 FPS | 프레임 | 1% low |
|---|---:|---:|---:|
| 1920×1080 High | 60.0 | 16.67 ms | 56.5 |
| 1280×720 High | 60.0 | 16.67 ms | 56.5 |
| 1920×1080 High / Storm | 60.0 | 16.67 ms | 56.8 |
| 1920×1080 Low / Storm | 60.0 | 16.67 ms | 56.8 |
| 1920×1080 High / 수중(Calm Lagoon) | 54.9 | 18.21 ms | 29.9 |
| 3840×2160 Ultra (여유 측정) | 19.0 | 52.64 ms | 15.0 |

수중이 60에 못 미치는 것은 이번 작업의 대가다. 석호 수중 시점에서는 해저 메시가 화면을
채우고, 그 재질이 이제 공유 `seabedAlbedo`/`seabedHeight`를 쓴다. 숨기지 않고 기록한다.

**스펙트럼 재검증**

- FFT 5/5 PASS (maxRel ≤ 4.0e-7), Hermitian 오차 0 (L=512/96/18),
  end-to-end Hs 1.951 m / 요청 2.0 m (비 0.976) PASS.

### Phase 14b — 히어로 오브젝트

15 m 세일링 보트를 절차적으로 추가했다(`src/props/vessel.js`). 선체는 스테이션 로프트,
리그는 실린더, 돛은 캠버 그리드. 도장은 부트 라인에서 갈라 선저/현측을 다른 색으로 준다.

**문제 23 — 강체 선체 밑으로 하늘이 보였다**

- 증상: 배가 물 위에 떠 있는 게 아니라 얹혀 있는 것처럼 보임. 선체 아래로 주변 수면이 비침.
- 원인: 앵커 한 점에서 파도를 샘플하는데 선체는 15 m다. 뱃머리 아래 스웰과 고물 아래 스웰이
  일상적으로 0.5 m 차이 난다.
- 수정: **소프트 헐**. 수면 아래 정점만 *자기 위치의* 수면 높이로 끌어당기고 현측에서 페이드아웃한다.
  갑판과 리그는 강체로 남아 피치·롤을 담당한다. 선체가 몇 cm 휘지만 그 휨은 물속에 있다.
  덧붙여 흘수를 2.05 m로 키우고 전체를 0.55 m 가라앉혔다.

**문제 24 — 포말 칼라가 배가 올라탄 뗏목처럼 보였다**

- 원인: 선체의 발자국을 월드 축 정렬 타원으로 계산했다. 헤딩이 57°일 때 바운딩 박스는 실제
  타원의 두 배 가까운 면적이다.
- 수정: 유니폼으로 (cos, sin)을 넘겨 **선체 자기 프레임으로 회전한 뒤** 정규화. 칼라 폭도
  좁히고 파도 포말과 같은 두 노이즈로 찢었다.

**측정**: 1920×1080 전 조건 60 fps 유지, 수중 55.8 fps(17.92 ms), 4K Ultra 52.82 ms. 콘솔 오류 0.

### Phase 14c — 다섯 각도 검수, 그리고 평면 반사

**보고했지만 실행조차 안 되던 것**: 소프트 헐. `world.y.assign(...)` — `toVar()`의 스위즐
대입은 TSL에서 오류 없이 컴파일되고 아무 일도 하지 않는다. 벡터 전체를 대입해야 한다.
각도 하나만 보고 "고쳤다"고 적은 결과였다. `qa.mjs`에 배 주위 5시점을 상설 뷰로 넣었다.

**문제 25 — 전부 같은 회청색 한 덩어리**. `paint` 속성을 직접 출력해 보니 색은 멀쩡했다.
넓은 앰비언트 + 페인트에 곱해지지 않는 하늘색 림 항이 모든 면을 같은 값으로 수렴시켰다.
Half-Lambert + 법선에 따라 변하는 반구 앰비언트로 교체. Half-Lambert는 로프트 패치의
와인딩이 뒤집혀도 셰이딩이 무너지지 않게 해준다.

**문제 26 — 부트 라인이 흘수선보다 1.2 m 위**. 단면 파라미터 고정값으로 갈랐기 때문.
높이는 스테이션마다 풀어야 한다. 건현 0.5 m → 1.5 m.

**문제 27 — 잠긴 선체까지 반사되어 바다 절반에 검은 얼룩**. 반사하면 수면 *위*로 올라온다.
`renderer.clippingPlanes`로 y=0 아래를 잘라냈다.

**문제 28 — 수중에서 하늘·반사 패스가 계속 돌았다.** 게이트가 `underwaterFactor < 1`인데
이 값은 damp되어 0.999…에서 멎는다. 한 번도 발동하지 않았다. `< 0.9`로 바꿨다.

**A/B 실측**: 반사 패스를 켜고 끄기를 번갈아 4회 — OFF 17.28/16.92, ON 16.67/17.89.
차이 0.18 ms로 노이즈 안이다. 그 사이 같은 설정이 16.67~21.01로 흔들렸는데, 연속으로
무거운 세션을 돌린 뒤의 열 문제다. `?noreflect=1`은 그 귀속을 위한 스위치다.

### Phase 14d — 항적, 그리고 선체 프레임 회전 버그

**문제 29 — 선체 프레임 회전이 처음부터 틀렸다**

- 증상: 포말 칼라와 선체 그림자가 선체를 정확히 감싸지 않고 어긋나 있었다.
- 원인: 월드 XZ를 선체 프레임으로 돌리는 전치 행렬의 부호가 한 항에서 틀려 있었다.
  `-h`가 아니라 `-2h`만큼 돈다. **헤딩이 0일 때만 맞는데, 그건 누구든 처음 시험해 볼 값이다.**
  검산으로 잡았다: 뱃머리 방향을 넣었더니 (0, 1)이 아니라 (-0.909, -0.416)이 나왔다.
- 수정: `[[c, -s], [s, c]]`.

**항적**: 계류 중인 배 뒤에 자국을 그리는 대신 배를 항주시켰다(약 3노트).
변위선체의 Kelvin 각은 속도와 무관하게 약 19.5°이고, 그것이 항적이 알아볼 수 있는
형태인 이유다. 두 팔은 그 고정 각으로 뻗고, 가운데 교란은 퍼지며 훨씬 빨리 사라진다.

**QA 부작용**: 배가 움직이자 고정 카메라 검수 뷰가 전부 빈 바다를 찍었다.
`__set({vesselHove:true})`로 정지시킬 수 있게 하고 `setState`에 넣었다 —
항적 유니폼은 건드리지 않으므로 정지 상태에서도 항적은 보인다.

**측정**: 1920×1080 전 조건 60 fps, 수중도 60 fps 복귀, 4K Ultra 52.02 ms. 콘솔 오류 0.

### Phase 14e — 절차적 판재와 돛 패널

정점마다 부위 ID(`part`)를 실어 보낸다. **법선으로는 알 수 없기 때문이다**: 판재는
현측에서 앞뒤로 흐르고 갑판에서는 뱃전을 가로지른다. 이걸 반대로 놓으면 배를 본 적 있는
사람에게는 즉시 틀려 보인다.

- 선체: 약 22 cm 판재. 이음선은 홈이므로 어두운 선 + 그 위 판재의 밝은 모서리.
  판재마다 색조를 흔든다 — 똑같은 판재만 늘어놓으면 조명이 아무리 좋아도 압출물로 읽힌다.
- 흘수선 때: 부트 스트라이프 주변은 어떤 배에서나 가장 더러운 곳이고,
  그 위의 얼룩 띠가 "일하는 배"와 "배 렌더링"을 가른다.
- 돛: 약 1.5 m 가로 패널 + 러프 쪽이 강한 느슨한 주름장.
- 스파: 약간의 결.

**항적 재조정**: 처음 값은 팔을 따라 흰 강물을 부었다. 양이 포말 마스크의 상단 임계값을
그대로 넘겨 포화됐기 때문이다 — 0.70 위는 전부 같아 보이므로 자국 전체가 가장자리 없는
한 덩어리로 뭉갰다. 3노트짜리 항적이면 그 절반이면 된다.

**측정**: 1920×1080 전 조건 60 fps, 수중 57.6 fps, 4K Ultra 59.02 ms. 콘솔 오류 0.

### Phase 14f — 실루엣

슬루프 → **케치**. 뒤쪽에 낮은 마스트 하나를 세우는 것이 실루엣에 쓸 수 있는 가장 싼 큰
변화다: 멀리서 슬루프는 삼각형 하나로 읽히는데 그걸 깨는 데 스파 하나·붐 하나·돛 하나면 된다.
개프(메인 헤드의 스파)를 더해 현대식 삼각형이 아니라 작업선으로 읽히게 했고,
현측을 따라 스탠션과 라이프라인을 돌렸다 — 작고 반복되고 눈이 가는 곳마다 있는,
"형상"과 "배"를 가르는 종류의 디테일이다.

전부 합쳐 **3,608 삼각형**. 1920×1080 전 조건 60 fps 유지, 수중도 60 fps, 4K Ultra 51.37 ms.
콘솔 오류 0.

### Phase 14g — 갑판 설비와 Orbit 카메라

**갑판 설비**: 조타륜(림은 스트럿 12개 링, 스포크 6 — 원반으로 만들면 접시로 읽힌다),
시트 윈치 4, 앞갑판 해치, 양묘기와 브라스 드럼, 콕핏 코밍. **실루엣은 하나도 바꾸지 않는다.**
전부 근접 시점용이고, 빈 갑판은 선체가 아무리 좋아도 "배의 모형"으로 읽힌다.
총 4,248 삼각형.

**Orbit 카메라** (`O`): 같은 yaw/pitch가 두 모드를 모두 구동한다. 드래그의 의미가 양쪽에서
같으므로 전환해도 시야가 손 밑에서 튀지 않는다. 차이는 막대의 어느 쪽을 잡느냐뿐이다 —
비행은 눈을 자기 축으로 돌리고, 궤도는 눈을 대상 주위로 휘두른다. 진입 거리는 지금 눈이
있는 거리를 그대로 쓴다(전환이 컷이 아니라 조작 변경이 되도록). 휠은 궤도에서는 돌리,
비행에서는 스로틀이다.

**수치 검증**: 궤도 진입 후 카메라–대상 거리 24.00(요청 24), 카메라 전방 벡터와 대상 방향의
내적 1.0000. 스크린샷으로 "맞아 보인다"가 아니라 실제로 정확히 겨눈다.

**측정**: 1920×1080 전 조건 60 fps, 수중 60 fps, 4K Ultra 52.07 ms. 콘솔 오류 0.

**남은 격차 (참조 대비, 정직하게)**

1. **여전히 작고 단순한 배다.** 참조는 3마스트 범선이다. 갑판 위 설비(조타륜·윈치·계류 장비)가
   없고, 조타·조종 없이 정해진 침로로만 항주한다. 반사는 해석적 하늘 전용이라 오브젝트를
   담지 못한다. 삭구선도 질감도 없는 단색 도장이라 참조의 범선보다 실루엣이 훨씬 단순하다. 참조는 범선 한 척과 그 흘수선 포말·선체 그림자·항적을 가진다.
   이쪽은 부표 9개뿐이고 접촉 표현이 없다. 스케일감과 "물과 무언가의 상호작용"이 통째로 빠져 있다.
2. **얕은 물의 결정질 대비가 부족하다.** 굴절이 파면 법선을 그대로 쓰기 때문에 바닥 무늬가
   픽셀 단위로 흩어져 평균화된다. 참조는 훨씬 또렷한 산호/모래 경계를 보여준다.
3. **카메라 모드가 fly 하나.** 참조는 Orbit / Fly / Boat 세 가지다.
4. **품질 티어 4단.** 참조는 Max를 더해 5단이다.

**결과**

- PASS. 콘솔 오류 0, 전 프리셋 캡처 정상, WebGL 2 폴백 정상, 스펙트럼 검증 통과.


### 판정 근거 요약

| 항목 | 근거 |
|---|---|
| 콘솔 오류 0 | 4개 경로 동시 수집, 모든 실행에서 0 |
| 진입점 단일성 | `index.html` 하나. 단일 파일 번들 요구는 사용자 지시로 완화 |
| 외부 자산 0건 | `performance.getEntriesByType("resource")`의 외부 호스트 = **0** |
| 파도 3스케일 | 밴드 λ 60–240 / 13–52 / 1.8–11 m, 캡처에서 식별 |
| 정점 뒤집힘 없음 | `Σk·H = choppy ≤ 1`로 구조적 보장 (설정으로 위반 불가) |
| 반사/투과 균형 | Schlick F0=0.02, 시선각에 따라 변화 |
| 태양·하이라이트 일치 | 하늘과 수면이 같은 `sunDir` uniform 사용, Golden Hour 캡처 |
| 프리셋 전환 | 6회 전환 최악 프레임 30.5 ms (기준 250 ms) |
| Moonlit 비검정 | 캡처에서 별·달빛 글리터·물결 식별 가능 |
| 수면 경계 안정 | 400스텝 왕복에서 전환 3회 = 이론적 최소, 떨림 없음 |
| 5분 메모리 | 17.1 → 18.7 MB, 진동하며 단조 증가 없음, 오류 0 |
| Low 티어 30 fps 이상 | Low/Storm 60.0 fps |
| 좁은 화면 | 420×780에서 패널이 canvas의 46.4%만 차지 |
| reduced-motion | 패널·힌트·토스트·부트 애니메이션 전부 `none`/`0s`로 확인 |
| 폴백 | `?forcewebgl=1` → backend `WebGL 2`, 오류 0, 345,827 tris |

### 남은 제한

`task.md`의 "알려진 제한" 7항목. 요약: IFFT 미구현(사용자 지시), 비/스프레이 없음,
부유 오브젝트 없음, 프리셋 URL 공유 없음, 치명적 오류 화면 강제 주입 미검증,
물기둥 god ray 없음, 모바일 미검증.

### 권장 다음 행동

1. 부유 오브젝트 추가 — 파도 스케일 감각에 가장 큰 영향
2. IFFT Ultra 경로를 별도 모드로 (Gerstner를 폴백 유지)
3. 저사양 GPU / 모바일 실측

---

## Current Status

- Phase: 1–6, 8 완료 / Phase 7(IFFT) 보류 / Phase 10 최종 QA 진행
- Overall: IN PROGRESS (기능 완성, 최종 검수 중)
- Active task: 최종 QA 및 문서 마감
- Last verified build: 2026-07-31, `index.html` + `src/**` (빌드 단계 없음)
- Renderer/backend: Three.js 0.185.1 · WebGPURenderer · **WebGPU** (강제 시 WebGL 2 폴백 동작 확인)
- Known blocker: 없음
- Next action: `acceptance.md` 최종 판정 기록

### 요구사항 완화 (사용자 승인, 숨기지 않고 기록)

- `prompt.md` 4·6번의 **"최종 제출물은 index.html 하나 / 마지막에 HTML로 번들"** 조건은
  사용자 지시로 **적용하지 않음**. 대신 `index.html`을 유일한 실행 진입점으로 유지하고
  로직은 `src/**/*.js` ES 모듈로 분리한다. 빌드 단계 없음.
  → `acceptance.md` BLOCKER "사용자 실행 진입점은 index.html 하나"는 그대로 충족.
- `plan.md` Phase 7(IFFT/FFT Ultra)은 사용자 지시로 **보류**. Gerstner가 최종 경로.

---

## Entry Template

### YYYY-MM-DD HH:MM — 짧은 제목

**목표**

- 

**변경**

- 파일:
- 내용:

**실행 환경**

- Browser/version:
- OS:
- GPU:
- Viewport:
- Three.js:
- Backend:
- Quality:

**검증**

- Command/server:
- Console:
- Visual:
- Average FPS/frame time:
- 1% low:
- Screenshot:

**문제**

- 증상:
- 재현:
- 원인:
- 시도:

**결과**

- PASS / PARTIAL / FAIL

**다음 행동**

1. 

---

### 2026-07-31 — Phase 12–13: 입체 구름 · 블룸 · 부유 오브젝트

**변경**

- `src/env/sky.js` — 하늘돔에 레이마치 구름 슬래브 추가(1500–4200 m). 단일 산란,
  HG 위상함수, 1탭 그림자 광선. 반사·대기 변형은 기존 평면 시트 유지
- `src/underwater/pipeline.js` — 수중 전용에서 **상시 포스트 파이프라인**으로 확장.
  1/4 해상도 브라이트 패스 + 분리형 9탭 가우시안 블룸
- `src/props/buoys.js` — 항로 표지 부표 9개. 인스턴싱

**부력 설계 — 리드백 없음**

부표는 수면과 **같은 캐스케이드 텍스처를 자기 정점 셰이더에서** 앵커 지점에 대해 샘플한다.
따라서 구성상 수면 위에 정확히 놓인다. 동기화가 어긋날 시뮬레이션도, GPU→CPU 리드백
지연도, 통계는 맞지만 위상이 다른 별도 CPU 파형 모델도 없다.
격자 단위로 개별 wrap하므로 한꺼번에 튀지 않고, wrap은 350 m 밖에서만 일어난다.

**문제 13 — 구름 밴딩**

- 증상: 하늘에 수직 줄무늬. 마칭 스텝 수가 그대로 그려짐
- 원인: 모든 광선이 같은 평면을 샘플. 밀도 임계도 지나치게 날카로웠음
- 수정: 픽셀별 지터로 첫 샘플 위치를 스텝의 일부만큼 밀고, 밀도 램프를 완만하게
- 재검증: 밴딩 소멸, 입체 적운으로 보임

**문제 14 — QA 훅이 `autoQuality`를 무시**

- 증상: 성능 측정 결과의 드로잉 버퍼가 1344×756. 요청한 1920×1080이 아님
- 원인: `window.__set()`이 preset/quality/camera/params/paused만 처리하고
  `autoQuality`는 조용히 버렸다. **모든 성능 수치가 자동 스케일러가 이미 낮춰 놓은
  해상도에서 측정되고 있었다** — 데이터처럼 보이지만 데이터가 아닌 종류의 측정
- 수정: 앱 설정 패스스루 추가. 이후 수치는 전부 재측정

**문제 15 — 구름 비용**

- 증상: 도입 직후 1080p High 19.2 ms, 4K Ultra 119.6 ms
- 원인: 전 해상도에서 픽셀마다 마칭. 2탭 그림자 광선이 1차 마칭만큼 비쌌음
- 수정: 그림자 1탭 2옥타브, 밀도 3옥타브, 스텝 축소(High 7 / Ultra 10),
  블룸의 전 해상도 RTT 제거(1/4 해상도에서 상류 그래프를 인라인)
- 재검증: 1080p High **16.67 ms(60 fps)** 회복

**문제 16 — WebGL 2 폴백 검은 화면 (회귀)**

- 증상: 폴백 캡처가 순수 검정. 콘솔 오류 0, 삼각형 346,910개 정상 렌더
- 원인: 블룸의 RTT 체인이 WebGL 2 백엔드에서 캔버스로 표시되지 않음
- 판단: 폴백은 쇼케이스가 아니라 호환 경로다. 막고 파고들기보다 **블룸을 WebGPU 한정**으로
  게이트하고 알려진 제한으로 기록했다
- 재검증: 폴백 정상 렌더 (`qa/qa-fallback.png`)

**실측 성능** (15초 샘플, autoQuality off — 이번엔 실제로 off)

| 조건 | 평균 FPS | 프레임 | 1% low |
|---|---:|---:|---:|
| 1920×1080 High | 60.0 | 16.67 ms | 56.8 |
| 1280×720 High | 60.0 | 16.67 ms | 56.8 |
| 1920×1080 High / Storm | 58.3 | 17.14 ms | 30.0 |
| 1920×1080 Low / Storm | 60.0 | 16.67 ms | 56.5 |
| 1920×1080 High / 수중 | 60.0 | 16.67 ms | 56.8 |
| 3840×2160 Ultra 여유 측정 | 16.3 | 61.29 ms | 15.0 |

정직하게: 4K Ultra 여유 측정이 구름 도입 전 19.54 ms → **61.29 ms**로 크게 나빠졌다.
레이마칭은 전 해상도 픽셀당 비용이라 4K에서 4배로 든다. 출하 설정에서는 60 fps지만,
Storm은 58.3으로 60에 못 미친다. 자동 품질이 켜져 있으면 해상도를 낮춰 흡수한다.

---

### 2026-07-31 — Phase 11: 스펙트럼 해양 (JONSWAP + GPU IFFT)

**목표**

- 상용 레퍼런스 급의 수면 디테일. Gerstner 20성분 합의 한계를 스펙트럼 방식으로 돌파

**보류 철회 근거**

사용자가 레퍼런스(threejswaterpro.com)를 지정하며 "최소 이 급"을 요구. 레퍼런스를
일반 사용자로서 관찰한 결과(코드·자산 추출 없음) 파라미터가 "wind speed / peak wavelength"
= 스펙트럼 모델이고, 근경 디테일 밀도가 사인파 합으로는 도달 불가능한 수준.
따라서 `plan.md` Phase 7 보류를 철회했다.

**변경**

- 추가: `src/ocean/spectrum.js`(JONSWAP h0 + CPU 참조 DFT), `src/ocean/fft.js`(Stockham IFFT
  compute), `src/ocean/spectral.js`(캐스케이드 오케스트레이션), `tools/fft-test.html`
- 수정: `src/ocean/material.js`(정점 변위 + **프래그먼트 단위 스펙트럼 경사**),
  `src/main.js`, `tools/qa.mjs`(`fft` 명령)

**설계**

- 3 캐스케이드 (L = 512 / 96 / 18 m), k 대역을 겹치지 않게 잘라 에너지 이중 계산 방지
- 복소 변환 1개에 실수 필드 2개를 패킹 → 실수 출력 6개가 변환 3개로 끝남
  (Dy·Dx / Dz·dY/dx / dY/dz·수평 발산)
- 발산 `∂Dx/∂x + ∂Dz/∂z`이 접힘 지표 → Gerstner의 해석적 Jacobian과 **같은 양**을
  포말 셰이더에 공급. 두 파형 엔진이 같은 인터페이스를 갖는다
- 프래그먼트에서 경사 텍스처를 재샘플 → 메시가 못 담는 디테일을 픽셀 단위로 복원
- **Gerstner 경로 유지**: compute가 없는 WebGL 2 폴백과 Low 티어의 엔진이자,
  CPU 수면 높이 질의의 출처

**검증 (시각이 아니라 수치로)**

| 검사 | 결과 |
|---|---|
| GPU FFT vs CPU DFT, N=8/16/64/256, 정·역변환 | 상대오차 8.3e-8 ~ 4.0e-7 = **float32 한계** |
| 스펙트럼 Hermitian 대칭 (3 캐스케이드) | 오차 **정확히 0** |
| 전 구간: 요청 Hs 2.0 m | 실측 **1.951 m** (비율 0.976) |

**문제 11 — 핑퐁 토글을 축마다 재시작**

- 증상: N=8 검증만 실패 (상대오차 1.002 = 100%)
- 원인: 스테이지 홀짝으로 버퍼를 고르면서 축마다 `s=1`로 재시작. `log2(N)`이 홀수면
  축 0의 결과가 `b`에 남는데 축 1이 `a`부터 다시 읽는다
- 왜 중요한가: 운영 크기 N=256(log2=8, 짝수)에서는 **우연히 맞아떨어져 보이지 않는다.**
  N=128로 바꾸는 순간 조용히 깨졌을 것이다
- 수정: 토글을 두 축에 걸쳐 연속으로 운용하고 결과 버퍼를 명시적으로 기록
- 재검증: N=8 상대오차 8.3e-8로 통과

**문제 12 — Hermitian 대칭 파괴**

- 증상: (사전 발견) conj(h0(-k)) 절반을 독립 난수로 생성하고 있었음
- 왜 중요한가: 복소 변환 1개에 실수 필드 2개를 태우므로, 허수부 오염이 잡음이 아니라
  **한 필드가 다른 필드로 새어 들어가는** 형태로 나타난다
- 수정: 2패스로 바꿔 미러 인덱스 `(N-x)%N`에 이미 저장된 값을 조회해 켤레로 채움

**성능 영향**

| 조건 | Gerstner | 스펙트럼 |
|---|---:|---:|
| 1920×1080 High | 16.67 ms (vsync) | 16.67 ms (vsync) |
| 4K Ultra 여유 측정 | 19.54 ms | 20.54 ms |

출하 설정에서는 비용이 vsync에 가려 보이지 않고, 부하 측정에서 약 1 ms.

**결과**

- PASS. 콘솔 오류 0, WebGL 2 폴백은 Gerstner로 정상 동작

---

### 2026-07-31 — Phase 2–6, 8: 광학 · 포말 · 수중 · 최적화

**목표**

- 수면 광학 마감, 포말 지속감, 수중 렌더링, 품질/복원력, 실측 가능한 QA 체계

**변경**

- 파일 추가: `tools/qa.mjs`(실 Chrome CDP 하네스), `src/ocean/foam.js`,
  `src/underwater/pipeline.js`, `src/underwater/scenery.js`
- 파일 수정: `src/ocean/{material,shading,waves}.js`, `src/env/{sky,uniforms,presets}.js`,
  `src/core/diagnostics.js`, `src/main.js`, `serve.mjs`(캡처 저장 엔드포인트)

**검증 환경**

- Browser: **Google Chrome / HeadlessChrome 151.0.0.0** (CDP 제어, 실제 rAF 구동)
- OS: macOS 26.5.2 · GPU: Apple M5 10-core
- Three.js 0.185.1 · Backend WebGPU (및 `?forcewebgl=1` 로 WebGL 2 검증)

**실측 성능** (각 30초 샘플, autoQuality off)

| 조건 | 평균 FPS | 평균 프레임 | 1% low | 드로잉 버퍼 | 삼각형 |
|---|---:|---:|---:|---|---:|
| 1920×1080 High | 60.0 | 16.67 ms | 53.8 | 1920×1080 | 345,827 |
| 1280×720 High | 60.0 | 16.67 ms | 56.8 | 1280×720 | 345,827 |
| 1920×1080 High / Storm | 60.0 | 16.67 ms | 56.8 | 1920×1080 | 345,827 |
| 1920×1080 Low / Storm | 60.0 | 16.67 ms | 56.8 | 1920×1080 | 80,833 |
| 1920×1080 High / 수중 | 60.0 | 16.67 ms | 56.5 | 1920×1080 | 394,819 |
| Ultra 여유 측정 | 51.2 | 19.54 ms | 29.8 | 2688×1512 | 591,459 |

정직한 해석: 출하 설정의 60 fps는 **vsync 상한**이지 GPU 한계가 아니다. 픽셀 수를
약 2배(2688×1512)로 올리고 Ultra 지오메트리를 쓴 마지막 행에서야 프레임이 16.67 ms를
벗어나므로, 1080p/High에는 상당한 여유가 있다.

**실측 안정성**

- 리사이즈 5회(1280×720 → 900×1400 → 1920×1080 → 640×480 → 1600×900): 오류 0, 이후 59.9 fps
- 프리셋 6회 전환 최악 프레임: 30.5 ms (첫 전환, 파이프라인 워밍) / 나머지 ~19.3 ms
  → `acceptance.md`의 "250 ms 이상 멈춤 없음" 충족
- 수면 경계 저속 왕복 400스텝(y 3.0 ↔ −1.0): 상태 전환 **3회**, 떨림 없음
- 숨김 탭 2.5초 후 복귀: 오류 0, 59.6 fps, 시간 점프 없음

**문제 5 — 포말이 페인트로 그린 리본처럼 보임**

- 증상: Jacobian crest 마스크가 파도 등고선을 그대로 따라가 길고 균일한 흰 띠가 됨
- 원인: 마스크만으로 실루엣을 결정했음. 실제 포말은 파도 능선의 *일부 구간*에서만 터진다
- 수정: 두 개의 상관 없는 노이즈 스케일로 침식 — 거친 것(≈12 m)이 어느 구간이 포말을
  만들지 결정하고, 고운 것(≈2.4 m)이 가장자리를 찢는다. 여기에 ping-pong 히스토리 추가
  (256 m 창 · 256² RGBA16F · 텍셀 단위 스냅 재투영 · 지수 감쇠 · 풍하 이류)
- 재검증: 파도가 깜빡이지 않고, 5초 전에 부서진 정상이 여전히 흔적을 남긴다

**문제 6 — 태양 하이라이트 이중 계산 → 중거리 올리브색 얼룩**

- 증상: 태양 방향 중거리에 탁한 올리브색 넓은 얼룩
- 원인: 반사 하늘의 좁은 글로우와 GGX 로브가 **같은 태양을 두 번** 그렸다. 블러된 글로우가
  넓고 흐린 워시로 깔려 청록 물빛과 섞이면서 채도가 죽었다
- 수정: 반사용 하늘 변형에서 좁은 로브 제거(넓은 Mie 헤일로는 유지), 태양은 GGX가 전담
- 재검증: 얼룩 소멸, 글리터 트랙이 밝고 선명해짐

**문제 7 — 반짝임이 생기지 않음 → Cox–Munk 도입**

- 증상: GGX 로브가 너무 좁아 픽셀 대부분이 로브를 빗나가고, 평균적으로 어두워짐
- 원인: 거칠기를 임의 상수로 키우고 있었음. 실제로는 근거리에서는 메시와 잔물결이 경사
  분포를 기하학적으로 만들고, 원거리에서는 한 픽셀이 수천 개 잔물결을 덮어 불가능해진다
- 수정: Cox & Munk 경험식 `σ² = 0.003 + 0.00512·U`로 해면 경사 분산을 구해
  `roughness = √(√2·σ)`를 원거리 목표값으로 두고 12–520 m 구간에서 보간.
  글리터 트랙 폭이 **튜닝 상수가 아니라 풍속에서** 나온다
- 재검증: 바람을 올리면 트랙이 넓어짐. 골든아워에서 태양 궤적이 선명하게 형성됨

**문제 8 — 해저가 렌더되지 않음 (검은 화면)**

- 증상: 수중에서 아래를 보면 완전한 검정
- 원인: `PlaneGeometry`는 XY 평면(z=0)인데 `positionGeometry.xz`를 사용 → 평면이 선으로 붕괴.
  또한 메시가 X축 −90° 회전이라 월드 Y 변위는 로컬 Z를 **음수로** 밀어야 한다
- 수정: `.xy`로 샘플링하고 `vec3(x, y, -height)` 반환
- 재검증: 해저 + 카우스틱 정상 표시, Beer–Lambert로 거리에 따라 물색에 녹아듦

**문제 9 — 스크린샷이 이전 프레임을 반환 (QA 방법론 결함)**

- 증상: 같은 코드로 같은 카메라를 캡처해도 실행마다 다른 이미지. "원양인데 해저가 보인다"
  같은 존재하지 않는 버그를 몇 시간 추적함
- 원인 (2단):
  1. `canvas.toDataURL()`은 **표시(presented)된 표면**을 읽는다. `render()` 직후 읽으면
     이전 프레임이 나온다. 인앱 브라우저는 페이지를 hidden으로 보고해 합성을 아예 안 하므로
     항상 낡은 프레임이었다
  2. WebGL 2 백엔드에서는 `preserveDrawingBuffer`가 없어 합성 후 드로잉 버퍼가 비워진다
     → 폴백 스크린샷이 간헐적으로 순수 검정
- 수정:
  - 페이지 쪽 `__capture()`는 `requestAnimationFrame` 2회를 기다린 뒤 읽도록 변경
  - **모든 QA 산출물은 CDP `Page.captureScreenshot`로 전환.** 합성기에서 직접 가져오므로
    두 백엔드 모두에서 정확하다
- 재검증: 9종 캡처가 실행 간 일관되게 재현됨. WebGL 2 폴백도 정상 이미지

**문제 10 — 수중 카우스틱을 화면 공간에 붙였음**

- 증상: 카우스틱이 카메라에 텍스처처럼 붙어 시선을 돌려도 따라오고, 바닥이 없는 원양에도 깔림
- 원인: `screenUV`로 샘플링. 월드 좌표로 투영해야 함
- 시도: 깊이에서 월드 좌표를 복원 — 역투영 행렬 규약(클립 z 범위, screenUV 상하 방향)이
  백엔드마다 달라 조용히 어긋났다. FOV로 직접 광선을 만드는 방식까지 갔으나 여전히 불안정
- 최종 수정: **포스트 패스에서 카우스틱을 제거.** 해저 머티리얼에 이미 월드 공간 카우스틱이
  올바르게 있고, 원양에는 바닥이 없어 물리적으로 존재할 이유가 없다. 중복 구현이 취약한
  기계장치를 요구하고 있었으므로 그 기계장치째 삭제하는 것이 옳았다
- 재검증: 스넬 창, 해저 카우스틱, 거리 흡수 모두 정상. 원양에는 카우스틱 없음(정확)

**결과**

- PASS. 프리셋 5종 + 수중 3종 + WebGL2 폴백 = 9종 캡처가 `qa/`에 재현 가능하게 저장됨

**다음 행동**

1. 5분 메모리 소크
2. `acceptance.md` 전 항목 판정
3. 문서 마감

---

### 2026-07-31 — Phase 1 수직 슬라이스

**목표**

- 렌더러 · 절차적 하늘 · 움직이는 수면 · 카메라 · resize · 오류 표시를 실제 브라우저에서 도는 상태로

**변경**

- 파일: `index.html`, `serve.mjs`, `vendor/three-0.185.1/`(4), `src/style.css`,
  `src/main.js`, `src/core/{util,renderer,diagnostics}.js`,
  `src/env/{uniforms,sky,presets}.js`, `src/ocean/{waves,geometry,shading,material}.js`,
  `src/input/controls.js`, `src/ui/panel.js`
- 내용: WebGPURenderer + TSL, 방사형 카메라 중심 메시(384링×448섹터, rim 60 km),
  20성분 3밴드 Gerstner(해석적 법선·Jacobian), 하늘/반사/대기 3변형 공유 절차적 하늘,
  Fresnel+Beer–Lambert+GGX 수면 광학, 프리셋 5종, 자체 UI 패널

**실행 환경**

- Browser: 인앱 Chromium 148 (`navigator.gpu` 사용 가능) / 교차확인 Google Chrome 151.0.7922.71
- OS: macOS 26.5.2
- GPU: Apple M5, 10-core
- Viewport: 1280×720 및 1920×1080, devicePixelRatio 2.0 → High 상한 1.5로 클램프
- Three.js: 0.185.1 (로컬 vendoring, 외부 요청 0건)
- Backend: WebGPU
- Quality: high (343k tris)

**검증**

- Command/server: `node serve.mjs` → http://localhost:8173
- Console: **오류 0, 경고 0** (`setConsoleFunction` 훅 + window.onerror + unhandledrejection 수집)
- Visual: 하늘·구름·수평선·다중 스케일 파동·정상 포말·깊이별 물색 확인
- Average FPS/frame time: **미측정** (아래 문제 2 참조 — 실 Chrome CDP 하네스 필요)
- Screenshot: 아직 파일로 저장하지 않음. Phase 10에서 `qa/`에 일괄 캡처

**문제 1 — 수면이 완전히 평평했음 (치명)**

- 증상: 파고를 1.8 m → 8 m로 올려도 수평선이 자로 잰 듯 직선. 화면의 물결은 전부
  프래그먼트 잔물결 노이즈였고, 정점 변위가 전혀 없었음.
- 재현: 어떤 waveHeight 값에서도 동일. CPU 미러는 정상(±1.04 m, Hs 검증 1.000)이라
  파라미터·정규화·분산관계는 모두 옳았음.
- 원인: **TSL `Loop()`와 `.toVar()`를 `Fn()` 콜백 밖에서 호출했음.**
  `Loop`는 `new LoopNode(...).toStack()`으로 빌더의 *현재 스택*에 붙는데, 모듈 스코프에는
  스택이 없어 루프 본문이 생성 셰이더에 아예 포함되지 않았다. 결과적으로 누산기가 전부
  초기값 0으로 남아 변위 0, 법선은 상수 (0,1,0)이 되었다. 컴파일 오류도 경고도 없어
  콘솔만으로는 절대 발견되지 않는 종류의 실패.
- 시도: 파형 파라미터 덤프 → 정상 확인 → GPU/CPU 불일치로 범위 축소 → 그래프 구축 시점 문제로 특정
- 수정: 파동 평가 전체를 `material.positionNode = Fn(() => { ... })()` 안으로 이동하고,
  프래그먼트로 넘길 값은 `varyingProperty()`로 선언 후 그 Fn 안에서 `assign()`.
  단일 스코프·단일 평가가 되어 변위·법선·Jacobian이 한 번에 나온다.
- 재검증: 파도가 정상적으로 나타남. waveHeight 슬라이더가 즉시 반응. 콘솔 오류 0 유지.

**문제 2 — 인앱 브라우저에서 화면이 갱신되지 않음**

- 증상: 파라미터를 바꿔도 스크린샷이 항상 동일. `waveTime`이 0.203에서 멈춤, `triangles: 0`.
- 원인: 인앱 브라우저 팬이 페이지를 `document.visibilityState === 'hidden'`으로 보고한다.
  숨겨진 페이지에는 브라우저가 `requestAnimationFrame`을 발행하지 않으므로 렌더 루프가
  단 한 번도 돌지 않았고, 모든 캡처가 로드 직후 프레임이었다. **앱 버그 아님.**
- 수정: `window.__advance(seconds, step)` QA 훅 추가 — 고정 스텝으로 update+render를
  수동 반복한다. 부수 효과로 캡처가 결정론적이 되어 같은 초 수는 항상 같은 해면을 만든다.
- 재검증: 시간이 진행되고 구름·파도가 움직임을 확인.

**문제 3 — 물이 금속판처럼 보이고 얇은 등고선이 그려짐**

- 증상: 골이 거의 검고 반사가 과해 크롬판 같았음. 근경에 따뜻한 색 얇은 폐곡선이 그물처럼 생김.
- 원인 (2가지 독립):
  1. 산란 스카이라이트 항이 없어 골이 흑색으로 붕괴. `waterDeep`만 보였음.
  2. 얇은 곡선은 **반사 경로**의 앨리어싱. `liftReflection`의 블렌드 밴드(0.10~-0.02)가
     근경 전체에서 활성이라 잔물결 법선이 매 픽셀 임계를 넘나들며 등고선을 그렸고,
     동시에 폭 ~5°짜리 태양 글로우를 거친 법선으로 점 샘플링해 필라멘트가 생겼다.
     (GGX 거칠기를 0.4로 올려도 사라지지 않아 스페큘러가 아님을 확인)
- 수정:
  1. `volume`에 상향 산란광 `waterScatter × skyLight × 0.8` 추가 → 물색이 하늘과 연동됨
  2. `liftReflection` 밴드를 (0.0 ~ -0.055)로 좁혀 실제 지평선 아래에서만 작동
  3. 하늘 함수에 `blur` 인자 추가 — 사전 컨볼브된 환경맵의 아날로그. 거칠기에 따라
     태양 글로우 지수를 붕괴시키고 구름 실루엣 램프를 넓힌다.
- 재검증: 필라멘트 소멸, 물이 물처럼 보임.

**문제 4 — 파도에 질감·반짝임이 없었음**

- 증상: 파형은 맞는데 표면이 매끈해 태양 반짝임이 생기지 않음.
- 원인: 엄밀한 Phillips 스펙트럼(단파 ∝ 1/k²)이 20성분 합에서는 에너지를 피크에만 몰아
  소규모 경사 분산이 0에 가까웠다. 글리터는 경사 분산에서 나온다.
- 수정: 단파측 지수 0.75 → 0.45로 완화, 밴드 배분을 chop 쪽으로 이동(0.30→0.40),
  chop 대역을 2.2–9.5 m → 1.8–11 m로 확장.
- 재검증: 표면 질감과 반짝임 발생. Hs 정규화는 유지되므로 총 파고는 불변.

**결과**

- PASS (Phase 1 완료 조건: 빈 화면·NaN·셰이더 컴파일 오류 없음 / UI가 canvas 입력을 막지 않음)
- 단, "1920×1080에서 60초 안정 실행"은 **미측정**. 인앱 브라우저에서 rAF가 돌지 않아
  실 Chrome CDP 하네스를 만든 뒤 측정해야 한다.

**다음 행동**

1. 실 Chrome CDP 하네스 작성 (rAF 기반 FPS 실측 + 파일 캡처)
2. Phase 2–3: 수면 광학 튜닝 (글리터 트랙, 프리셋별 검증)
3. Phase 4: 포말 ping-pong 히스토리

---

## Initial Entry

### 2026-07-31 — Agent kit initialized

**목표**

- 단일 HTML WebGPU 해양 데모를 위한 구현 지시 체계 준비

**변경**

- 구현 전용 문서 세트 생성

**검증**

- 아직 코드 또는 브라우저 검증 없음

**결과**

- NOT STARTED

**다음 행동**

1. `prompt.md`, `plan.md`, `task.md`, `acceptance.md`, `research.md` 읽기
2. 공식 Three.js API 재확인
3. Phase 1 최소 장면 구현

