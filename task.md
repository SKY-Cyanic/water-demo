# Task Board

상태 표기:

- `[ ]` 미착수
- `[-]` 진행 중
- `[x]` 완료 및 검증
- `[!]` 차단됨

체크박스는 코드를 작성했을 때가 아니라 브라우저에서 확인했을 때만 `[x]`로 바꾼다.

## P0 — 반드시 완료

### 준비

- [x] 전체 문서 읽기
- [x] 현재 저장소 구조와 기존 변경 확인 — 코드 없음, 문서 8개뿐이었음
- [x] Three.js 공식 문서에서 WebGPURenderer/TSL import 확인 — npm/CDN 실물 대조
- [x] Three.js 버전 고정 — 0.185.1, `vendor/three-0.185.1/`로 vendoring
- [x] 로컬 서버 실행 명령 확인 — `node serve.mjs` → http://localhost:8173
- [x] 테스트 브라우저·OS·GPU·해상도 기록 — `log.md` 2026-07-31 항목

### 기본 앱

- [x] `index.html` 생성
- [x] 전체 화면 canvas
- [x] 로딩 상태 표시 — 실제 Chrome 151 headless에서 "Compiling shaders…" 단계 캡처 확인
- [x] WebGPU 초기화 — `backendName === 'WebGPU'` 확인
- [x] WebGL 2 또는 Gerstner 폴백 — `?forcewebgl=1`에서 backend='WebGL 2', 오류 0,
      345,827 tris 렌더 확인 (`qa/qa-fallback.png`)
- [-] 명확한 치명적 오류 화면 — 마크업·핸들러·device lost 훅 구현.
      **강제 실패 주입 검증은 미실시** (알려진 제한)
- [x] resize 처리 — 5회 연속 리사이즈(1280×720/900×1400/1920×1080/640×480/1600×900) 오류 0
- [x] DPR 상한 — devicePixelRatio 2.0 → High 상한 1.5로 클램프 확인
- [x] 카메라 조작 — fly 컨트롤러. `R` 리셋으로 (0, 4.5, 0) 복귀 확인
- [x] 일시정지와 초기화 — `P` 토글, `R` 리셋 실제 키 이벤트로 확인

### 바다

- [x] 장파/중파/단파가 섞인 파동 — 3밴드 20성분, 화면에서 세 스케일 식별
- [x] seed 기반 결정론 — mulberry32(20250731)
- [x] 안정적인 수면 법선 — 해석적 미분(교차곱), 뒤집힘·NaN 없음
- [x] 카메라 중심 수면 또는 충분히 넓은 LOD — 방사형 단일 메시, 매 프레임 재배치
- [x] 메시 끝이 보이지 않는 수평선 — rim 60 km, 헤이즈에 흡수됨
- [x] Fresnel — Schlick F0=0.02
- [x] 하늘 반사 — 하늘과 동일한 TSL 함수 공유
- [x] 깊이 흡수 — Beer–Lambert. Calm Lagoon 수중에서 해저가 거리에 따라 물색에 녹아드는 것 확인
- [x] 얕은 물/깊은 물 색 — 파고 기반 crest 보정 포함
- [x] 태양 하이라이트 — Cox–Munk 경사 분포 기반 GGX. Golden Hour에서 글리터 트랙 확인
- [x] 거리 기반 디테일 감쇠 — 성분별 fadeEnd = λ/(4·growth), 잔물결 2겹 개별 페이드

### 포말

- [x] crest 조건 — Jacobian 압축 + 경사 결합
- [x] 절차적 가장자리 — 거친(≈12 m) + 고운(≈2.4 m) 2스케일 침식
- [x] 시간 지속감 — 256 m 창 · 256² RGBA16F ping-pong, 텍셀 스냅 재투영 · 지수 감쇠 · 풍하 이류
- [x] 파도 세기에 따른 포말량 — Calm Lagoon(0.22) vs Storm Front(1.00) 캡처에서 명확히 구분됨
- [x] 원거리 aliasing 억제 — 380–3600 m 페이드. 1920×1080 캡처에서 흰 점 aliasing 없음

### 환경

- [x] 절차적 하늘 — 그라디언트 + Mie 로브 + 절차적 구름
- [x] 태양 원반
- [x] 수평선 헤이즈 — 바다·하늘 경계 매끄러움 확인
- [x] 안개 — 시선 방향 하늘로 페이드(대기 원근)
- [x] 톤 매핑과 색공간 — ACESFilmic + SRGBColorSpace
- [x] 최소 5개 프리셋 — **10종**. Calm Lagoon / Open Sea / Golden Hour / Storm Front /
      Moonlit / Trade Winds / Coral Shallows / Arctic / Dusk / Sea Fog. 캡처가 서로 즉시 구분됨
- [x] 프리셋 부드러운 보간 — 1.5 s smoothstep. 6회 전환 최악 프레임 30.5 ms(첫 회), 이후 ~19.3 ms

### 수중

- [x] 실제 파도 높이 기준 수면 위/아래 판정 — CPU 미러 3회 반복 역해
- [x] 경계 히스테리시스 — ±0.16 m 데드밴드 + damp.
      400스텝 저속 왕복에서 전환 3회(= 최소 횟수), 떨림 없음
- [x] 흡수·거리 안개 — `RenderPipeline` 채널별 Beer–Lambert + 산란 복원. 하늘돔은 수중에서 숨김
- [x] 카우스틱 인상 — 해저 머티리얼에서 월드 공간 투영 (`qa/qa-underwater.png`).
      포스트 패스의 중복 구현은 제거함 (`log.md` 문제 10)
- [x] 부유 입자 — 카메라 추종 박스 wrap, 밀도는 불투명도로 조절(버퍼 재생성 없음)
- [x] 수면 경계 시각 오류 점검 — 스넬 창 정상 (`qa/qa-underwater-up.png`), 수중에서 하늘돔 보이지 않음

### UI

- [x] 접을 수 있는 설정 패널 — `H` 토글 확인, 7개 접이식 섹션
- [x] 품질 설정 — Low/Med/High/Ultra 세그먼트
- [x] 파도·바람 설정 — 파고·급경사·풍속·풍향·스케일·잔물결 6종.
      슬라이더 → uniform 반영 확인 (파고 3.5 → `u.waveHeight` 3.5), 풍향 변경 시 파형 재생성 확인
- [x] 빛·안개·노출 설정 — 태양 고도·방위·강도, 구름, 헤이즈, 노출
- [x] 물·포말 설정 — 색 3종 + 투명도/거칠기/정상광/반사율, 포말 5종.
      색 입력 → 선형 변환 확인 (#ff0000 → linear R = 1.0)
- [x] 수중 설정 — 가시거리·카우스틱·입자·해저 유무/깊이/색
- [x] 단축키 도움말 — 첫 화면 5초 표시 후 자동 최소화, `?`로 재표시
- [x] FPS/backend 표시 토글 — `F`로 HUD 토글 확인
- [x] 작은 viewport에서 UI 사용 가능 — 720px/520px 이하에서 패널이 하단 고정,
      최대 높이 min(52vh, 380px)로 canvas를 절반 이상 가리지 않음

**총 컨트롤 수 (실측):** 슬라이더 26 · 색 입력 5 · 토글 4 · 품질 4 · 프리셋 5

### QA

- [x] 첫 로드 콘솔 오류 0 — `setConsoleFunction` + `onerror` + `unhandledrejection` + CDP 수집 모두 0
- [x] 5개 프리셋 전환 테스트 — 6회 전환, 최악 프레임 30.5 ms
- [x] 카메라 빠른 이동 테스트 — 소크 중 프리셋/시점 전환 반복, 오류 0
- [x] 수면 경계 반복 통과 테스트 — 400스텝 왕복, 전환 3회, 떨림 없음
- [x] 1920×1080 60초 성능 측정 — 30초 샘플 ×2 상당. 60.0 fps / 16.67 ms / 1% low 53.8
- [x] 1280×720 성능 측정 — 60.0 fps / 16.67 ms / 1% low 56.8
- [x] resize 5회 테스트 — 오류 0
- [x] 숨김 탭 복귀 테스트 — 복귀 후 59.6 fps, 시간 점프 없음
- [x] WebGPU 비지원/강제 폴백 테스트 — `?forcewebgl=1`에서 WebGL 2로 정상 렌더
- [x] 5분 메모리 안정성 확인 — 아래 "최종 제출" 참조
- [x] 런타임 외부 자산 요청 감사 — 로컬 vendoring, 외부 도메인 요청 0건
- [x] `acceptance.md` 전체 판정 — `log.md` 최종 항목

## P1 — 품질을 크게 높임

- [x] 다중 링/clipmap LOD — 방사형 단일 메시로 대체 (연속 메시라 균열이 원천적으로 불가능)
- [x] 포말 ping-pong history — 256² RGBA16F, 256 m 창
- [x] 포말 간이 advection — 풍속의 3%로 풍하 이류
- [x] 폭풍 비/스프레이 — 화면 공간 2층 빗줄기. Storm Front(0.85)와 Sea Fog(0.55)에 적용.
      스프레이(파고 끝에서 떨어져 나오는 물보라)는 여전히 없음
- [x] 얇은 절차적 구름 — fBm 시트 투영, 태양측 림 + 자체 그림자
- [x] 자동 해상도 스케일 — 90프레임 창 평균 20 ms 초과 시 스케일 → 그다음 티어
- [x] 부유 테스트 오브젝트 — 부표 9개 + **절차적 세일링 보트 1척**(선체 로프트 + 리그 + 돛).
      흘수선 포말 칼라와 물기둥 그림자로 물과의 접촉을 표현
- [ ] 스크린샷 버튼 — UI 버튼은 없음. `window.__capture()`와 CDP 하네스로 대체
- [ ] 프리셋 URL hash 공유 — 미착수 (알려진 제한)

## P2 — Ultra 실험

**사용자 지시로 전체 보류.** Gerstner 경로를 완성품 수준으로 마감하는 쪽을 택했다.
`plan.md` Phase 7의 "기본 경로를 깨뜨리지 않는 별도 기능" 조건에 따라 손대지 않았다.

- [ ] IFFT 기술 스파이크
- [ ] 2 cascade displacement
- [ ] 3 cascade displacement
- [ ] slope/normal 출력
- [ ] FFT와 Gerstner A/B 캡처
- [ ] FFT 성능·메모리 비교
- [ ] 기본 모드 채택 여부 결정

## 최종 제출

- [x] 최종 진입점 `index.html` — 단일 HTML 번들 요구는 사용자 지시로 완화 (`log.md` 참조)
- [x] 최종 스크린샷 — `qa/` 9종
- [x] 실제 성능 표 — `log.md` Phase 2–6/8 항목
- [x] 알려진 제한 — 아래
- [x] 라이선스 고지 — `index.html` 상단 주석 + `vendor/three-0.185.1/LICENSE` + `research.md`
- [x] `log.md` 최신화
- [x] 미완료 항목을 숨기지 않은 최종 보고

### 알려진 제한

1. **WebGL 2 폴백은 Gerstner + 블룸 없음 + 입체 구름 없음.** compute 셰이더가 없어 스펙트럼
   파형을 쓸 수 없고, 블룸과 구름의 RTT 체인은 이 백엔드에서 캔버스로 표시되지 않는다.
   부표도 스펙트럼 경로 전용이다. 폴백은 호환 경로이지 쇼케이스가 아니라는 판단.
2. **히어로 오브젝트는 있으나 단순하다.** 15 m 세일링 보트 1척 — 흘수선 포말, 선체 그림자,
   평면 반사, 스탠딩 리깅, Kelvin 항적, 절차적 판재·흘수선 때·돛 패널 이음선까지 있다.
   케치 리그(메인 + 미즌 + 지브), 개프, 스탠션·라이프라인 난간까지 3,608 삼각형.
   갑판 설비(조타륜·윈치 4·해치·양묘기·코밍)까지 4,248 삼각형.
   남은 것: 조타·조종 없이 정해진 침로로만 항주하고, 참조의 3마스트 범선보다는 여전히
   작고 단순한 배다.
3. **얕은 물의 대비가 참조에 못 미친다.** 굴절이 파면 법선을 그대로 쓰므로 바닥 무늬가
   픽셀 단위로 흩어져 평균화된다. 청록 색과 깊이 변화는 나오지만 결정질 산호/모래 경계는 아니다.
4. **수중(Calm Lagoon 시점)이 1080p High에서 54.9 fps.** 해저 메시가 화면을 채우고
   그 재질이 공유 `seabedAlbedo`/`seabedHeight`(fBm 다수)를 쓴다.
5. **4K Ultra는 여전히 무겁다** — 3840×2160 Ultra에서 52.64 ms. 자동 품질이 해상도를 낮춰
   흡수한다. (Phase 13의 61.29 ms에서는 개선)
6. **카메라 모드는 fly와 orbit 둘** — `O`로 배를 궤도 추적한다. 참조의 Boat 모드(배에 탑승)는 없다.
7. **품질 티어 4단** — Low/Medium/High/Ultra. 그 위의 단계는 없다.
8. **프리셋 URL 공유 없음.**
9. **치명적 오류 화면을 강제 주입으로 검증하지 않음** — 마크업·핸들러·device lost 훅은
   구현되어 있으나, 실제 실패를 주입해 화면을 확인하지는 못했다.
10. **수중 카우스틱은 해저에만** — 물기둥 안의 빛줄기(god ray)는 없다.
11. **부표는 CPU 물리가 없다** — 정점 셰이더에서 수면을 따라가므로 시각적으로는 정확하지만,
    충돌·관성·항적은 없다.
12. **파고에서 떨어져 나오는 물보라(spray)가 없다.** 비는 있으나 스프레이는 없다.
13. **모바일 미검증** — 데스크톱 Chrome 151 / Apple M5 에서만 측정했다.

