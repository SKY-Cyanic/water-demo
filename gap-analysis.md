# 상용 참조와의 근본 차이 — 조사 결과

2026-08-02. 대상: Three.js Water Pro (threejswaterpro.com).

## 0. 조사 방법과 경계

`research.md` §4를 그대로 따랐다.

- **한 것**: 일반 방문자로 데모 페이지를 열어 관찰, 공개 문서 사이트
  (`docs.threejswaterpro.com`)의 API 레퍼런스·체인지로그 열람, 공개 기법 문헌 대조.
- **하지 않은 것**: 번들 디컴파일, 비공개 셰이더·텍스처·모델·설정 추출,
  네트워크 요청에서 자산 저장. 관찰용 스크린샷은 스크래치패드에만 두고
  저장소에 넣지 않았으며 텍스처·배경으로 쓰지 않는다.
- 이 문서의 모든 항목은 **공개 정보 또는 화면 관찰**에서 나온 것이고,
  구현 방법은 전부 공개된 일반 기법으로 우리가 독자적으로 재작성할 대상이다.

## 1. 같은 것 — 여기는 격차가 아니다

공개 문서가 명시한 그들의 기반은 우리와 사실상 같은 계열이다.

| 항목 | 그들 | 우리 |
|---|---|---|
| 파동 | FFT, JONSWAP 스펙트럼 | FFT, JONSWAP 스펙트럼 |
| 캐스케이드 | 3단 (swell / waves / ripples) | 3단 (512 / 96 / 18 m) |
| 파괴 판정 | Jacobian 기반 | Jacobian 기반 |
| 지오메트리 | clipmap + LOD | 동심 링 + LOD |
| 흡수 | Beer–Lambert 채널별 | Beer–Lambert 채널별 |
| 물리 단위 | 실제 m/s로 보정 | Hs 실측 1.948 m / 요청 2.0 m |
| 부력 | 있음 | 4점 평면 피팅 |
| 백엔드 | TSL + WebGPU, WebGL 폴백 | TSL + WebGPU, WebGL 2 폴백 |

**즉 파동 시뮬레이션은 격차가 아니다.** 우리가 뒤지는 곳은 전부 그 위쪽,
"물을 어떻게 보이게 하는가"에 있다.

## 2. 근본 차이 — 관찰에서 나온 한 문장

> **그들의 물은 창(window)이고, 우리 물은 표면(surface)이다.**

참조 데모의 전경에서는 산호초·모래·깊은 수로가 **거의 검정부터 밝은 청록까지의
전체 계조로** 보이고, 그 무늬가 수면 파도에 따라 굴절되어 흔들린다. 동시에 수면
자체의 스페큘러와 잔물결이 그 위에 얹힌다. 물이 **투명한 매질**로 작동한다.

우리 것은 같은 조건(Coral Shallows)에서 바닥이 **옅은 세척된 톤**으로만 나오고
대비가 거의 없다. 물이 **색이 칠해진 판**으로 작동한다.

### 원인 (코드에서 확인)

`src/main.js:137` 은 해저 메시를 씬에 넣어 **실제로 렌더한다**.
그런데 `src/ocean/material.js:51` 의 수면은 `transparent = false` 인 불투명 재질이라
그 위에 그려지며 해저를 완전히 덮는다. 그리고 같은 셰이더가
`seabedHeight()` / `seabedAlbedo()` 로 **바닥을 해석적으로 다시 만들어** 대체한다.

**같은 바닥을 두 번 그리고, 둘 중 나쁜 쪽을 화면에 내보내고 있다.**

해석적 재구성의 한계는 구조적이다:

1. 실제 지오메트리가 없으므로 그림자·차폐·정확한 형태가 없다.
2. `seabedAlbedo` 가 돌려주는 값은 `sand × (0.30 + grain×0.22)`, 잡초가
   `× (0.14, 0.24, 0.20)` — **동적 범위가 좁다.** 실제 리프의 "검은 수로 대 밝은 모래"
   대비가 애초에 만들어지지 않는다.
3. **수몰된 물체를 전혀 보여줄 수 없다.** 선체의 수면 아래 부분, 부표의 잠긴 부분이
   위에서 볼 때 사라진다. 참조에서는 보인다.
4. `volume` 인스캐터가 전 화면에 같은 세기로 더해져 바닥을 **균일하게 뿌옇게** 만든다.
   참조의 근거리 물은 실제로 얇아서 베일이 거의 없다.

참조 체인지로그의 표현이 정확히 이것을 가리킨다:
*"Local Fresnel-based transparency: see-through thin water from above; opaque at
grazing angles."*

## 3. 격차 목록 — 영향 순

### G1. 굴절이 렌더가 아니라 가짜다 ★★★★★ — **완료**

위 §2. 이것 하나가 정지 화면 인상의 대부분이다.

**좁히는 법**: 수면을 그리기 전에 씬(해저 + 수몰 지오메트리)을 타깃에 렌더하고,
수면이 **굴절된 화면 UV로 그 타깃을 샘플**하게 한다. 표준 기법이고 새 발명이 아니다.

우리 쪽 사정이 유리하다: `src/underwater/pipeline.js` 가 이미 `pass(scene, camera)` 로
씬 컬러 타깃을 만든다. 수중에서만 켜지는 그 구조를 **수상에서도 쓰도록** 돌리면 된다.
필요한 것은 (a) 수면을 씬에서 분리해 나중에 그리기, (b) 굴절 UV = `screenUV + 표면 경사 ×
깊이 비례 계수`, (c) 굴절 샘플이 수면보다 앞의 물체를 집어오는 경우의 폴백.

`seabedHeight`/`seabedAlbedo` 는 **수중 패스와 지오메트리 생성용으로 남기고**,
수면의 해석적 바닥 경로는 삭제한다.

비용: 풀스크린 타깃 1장 + 샘플 1회. 현재 수상 60 fps에 여유가 있다
(1080p High가 vsync 상한이므로 실제 여유는 미측정 — `?noshafts=1` 식 킬 스위치로 재야 한다).

### G2. 인스캐터가 근거리까지 균일하게 뿌옇다 ★★★★ — **완료**

`volume` 이 `bodyLight` 와 `skyLight` 로 항상 같은 양 더해진다.
Beer–Lambert 투과 `T` 는 바닥이 있을 때만 살아 있고(`× u.seabedMix`),
바닥 없는 8개 프리셋에서는 인스캐터가 전부다.

**좁히는 법**: 인스캐터를 **시선이 지나온 물의 두께**에 비례시킨다.
G1이 들어오면 굴절 샘플까지의 실제 광학 거리가 생기므로 자연스럽게 따라온다.

### G3. 스프레이가 없다 ★★★★ — **완료**

참조 체인지로그: *"Sea spray emitters — 16 emitters with 32 probes"*.
깨지는 마루에서 튀어 흩날리는 물보라. **우리는 전혀 없다.**
Storm Front에서 가장 크게 벌어지는 항목이고, 정지 화면보다 **움직일 때** 차이가 크다.

**좁히는 법**: 마루 마스크가 임계를 넘는 지점에서 GPU 파티클을 방출.
`foam.js` 가 이미 프레임마다 마루 위치를 알고 있으므로 방출점은 공짜다.

### G4. 포말이 한 층뿐이다 ★★★ — **완료**

참조는 **세 층**: 마루 백파 / 상시 표면 포말 / 해안선 포말.
우리는 마루 백파 + 항적 + 흘수선 칼라 — 즉 **상시 표면 포말이 없다.**
실제 바다에는 바람이 만든 옅은 포말 줄무늬가 항상 깔려 있고, 그게 없으면
파도 사이 물이 지나치게 깨끗해 보인다.

**좁히는 법**: 풍속에 비례하는 저주파 줄무늬 포말을 풍향으로 늘려 깔고,
포말 히스토리의 남는 채널(`.b`)에 넣는다. `.r` 백파, `.g` 항적, `.b` 상시.

### G5. 반사가 절차적 하늘 + 40 m 평면 패스뿐이다 ★★★

참조는 SSR을 갖고 있다(`ssr` 옵션). 우리 `PropReflection` 은 배 전용이고
`reflNear` 로 40 m 안쪽에만 적용된다. 그 밖의 모든 것은 절차적 하늘만 반사한다.

**좁히는 법**: G1이 씬 컬러 타깃을 만들면 SSR의 절반은 이미 갖춰진다.
다만 SSR은 비용과 아티팩트가 크므로 G1~G4 뒤로 미룬다.

### G6. 항적이 포말일 뿐 변위가 아니다 ★★ — **완료**

참조: *"dispersive ship wakes"* — **수면이 실제로 변형된다**.
우리 항적은 포말 히스토리에만 있고 높이장은 건드리지 않는다.

**좁히는 법**: 항적을 캐스케이드 변위에 더하는 별도 변위 필드로.
분산 관계(짧은 파가 뒤처짐)를 지키지 않으면 켈빈 각이 깨진다.

### G7. 카우스틱이 그림자에 가려지지 않는다 ★★ — **완료**

참조: *"Sun-shadow occluded caustics"*. 선체 아래 바닥에 카우스틱이 계속 밝게 남는
우리와 달리, 그림자 안에서는 사라진다.

**좁히는 법**: 이미 `hullSunOcc` 를 갖고 있다. 같은 타원을 해저 카우스틱에도 곱한다.
**가장 싸다.**

### G8. 미세 스페큘러 밀도 ★★ — **완료**

참조 근거리 수면은 작고 날카로운 반짝임이 조밀하다. 우리 것은 더 넓고 뭉툭하다.
`rippleSlope` 의 미세 옥타브가 30~400 m에서 페이드아웃되는데,
그 페이드가 근거리에서도 진폭을 억제하고 있을 가능성.

**좁히는 법**: G1~G4 뒤에 `sparkle` 상당의 전용 항으로 재검토.
지금 건드리면 앨리어싱 회귀 위험이 크다(이번 세션에 이미 한 번 겪었다).

## 3b. 완료 기록 (2026-08-02)

- **G7** — 선체 그림자 타원을 해저 지점에서 다시 평가해 카우스틱에 곱한다.
  반음영은 빛이 통과한 물 깊이에 따라 넓어진다.
- **G1** — 수면을 투명 패스로 옮기고 `viewportSharedTexture`로 뒤 프레임을
  굴절 샘플. 아날리틱 바닥 경로는 `?norefract=1`에만 남는다.
  **페어드 A/B: 굴절 쪽이 1.63 ms 빠르다** — 아날리틱은 픽셀당 fBm 14옥타브 +
  Worley 2회이고 굴절은 텍스처 페치 1회다. 최대 시각 개선이 프레임 시간을 돌려줬다.
  부수 결함 3건(수면 앞 픽셀 흡입 → 깊이 비교 거부 / 화면 가장자리 줄무늬 →
  경계 테이퍼 / 수몰 선체 시안 발광 → 카우스틱을 페인트에 비례)도 각각 처리.
- **G4** — 윈드로우. 풍향에 평행한 저주파 줄무늬 포말. 이방성 노이즈로 구성하므로
  별도 방향 필터가 필요 없다. 4 m/s에서 켜지기 시작해 Calm Lagoon은 그대로 깨끗하다.
- **G3** — 스프레이. CPU 리드백 없이, 카메라를 따라가는 격자의 각 입자가
  **표면·포말 히스토리와 같은 접힘 지표**를 자기 앵커에서 샘플해 부서지는 동안만
  보인다. 8 m/s 미만에서는 마루가 찢기지 않으므로 꺼진다.

플리커 회귀 없음 (open-sea 3.22% 대 기준선 3.27%, storm 10.87% 대 10.78%).

- **G2** — 굴절과 같은 문제였다. `seabedMix`로 게이트하는 바람에 바닥 없는 8개
  프리셋은 프레임버퍼 복사 비용만 내고 이득이 0이었고, 수몰 선체도 안 보였다.
  물 두께를 **깊이 버퍼**에서 구하도록 바꾸니 둘이 같이 풀렸다. 뒤에 아무것도
  없으면 분리 거리가 거대해져 투과율이 0이 되고 순수 부피 색이 남는다 —
  외해 케이스가 일반 케이스에서 떨어져 나온다.

### 굴절 비용 (페어드 A/B, 냉각 후)

두 숫자가 **서로 다른 것을 잰다.** 하나로 합쳐 말하면 틀린다.

| 조건 | off | on | 페어드 중앙값 |
|---|---|---|---|
| 1080p, Coral Shallows (바닥 있음) | 29.13 ms | 27.82 ms | **−1.63 ms** |
| 4K, Open Sea (바닥 없음) | 24.84 ms | 30.58 ms | **+2.56 ms** |

- 바닥이 있으면 굴절이 **아날리틱 바닥을 대체**한다 — fBm 14옥타브 + Worley 2회가
  텍스처 페치 1회로 바뀌므로 순이득이다.
- 바닥이 없으면 굴절은 **순수 추가**다. 4K의 2.56 ms는 픽셀 1/4인 1080p에서
  약 0.6 ms이고, 그래서 1080p A/B에서 vsync 상한 아래로 사라졌다.
- 1080p Open Sea는 양쪽 arm 모두 깨끗한 60 fps. 앞서 걱정한 "복사 때문에 52.8"은
  데워진 기계였다.

- **G8** — 공개 문서가 정확히 알려줬다: 별도의 `sparkle` 항이고
  *"A view-dependent specular highlight scaled by the surface normal, with
  distance-based fade"*, `power: 512`, `minDistance: 10`, `fadeDistance: 500`.
  우리 GGX 로브는 픽셀이 덮는 경사 **분산**이라 "평균적으로 얼마나 밝은가"에
  답할 뿐 "몇 개 면이 정확히 태양을 향한다"에는 답할 수 없다. 지수 512(약 1°)의
  둘째 로브를 얹었다.
  **거리 창이 이 항을 안전하게 만드는 전부이고, 방향이 직관과 반대다** —
  근거리에서는 끄고(메시와 잔물결이 실제 면을 이미 해상한다) 원거리에서도 끈다
  (1° 로브를 픽셀 법선에 걸면 순수 앨리어싱이다). 면이 실재하지만 더는 해상되지
  않는 띠에만 존재한다. 플리커 회귀 없음(3.17 % / 0.303 % / 10.90 %).

## 3c. 공개 문서 전면 스캔에서 얻은 남은 격차 사양 (39개 페이지)

### G6 — 항적은 **격자 위의 실제 높이장**이다

문서가 구조를 그대로 준다. 우리 것(포말 히스토리에만 존재)과 종류가 다르다.

- `resolution: 512` 텍셀/변, `worldSize: 700` m, **카메라 중심**
- 물체는 `addGenerator(Object3D)`로 등록, 프레임당 최대 16개가 주입
- `depth: 1.2` m — *"Hull depth. The only control of wake amplitude"*
- `radius: 4.0` m — *"Footprint radius over the swept path. Controls the wake width"*
- `friction: 0.25` — *"Velocity damping. Higher values produce a shorter, more damped trail"*
- `foamBreakThreshold` — **`|∇h|`가 임계를 넘으면** 포말 침착
- `teleportThreshold: 5.0` m — 큰 프레임 간 이동은 순간이동으로 처리
- 높이장이라 **부력이 그것을 읽는다** — 물체가 서로의 항적에 반응한다

즉 해석적 켈빈 도형이 아니라 **감쇠 파동방정식을 푸는 512² 핑퐁 필드**다.
포말은 그 필드의 기울기에서 파생되지 별도로 그리지 않는다.
우리가 만들 것도 같은 형태여야 한다 — 그러면 켈빈 각이 저절로 나온다.

### G5 — SSR

- `strength: 0.8` — 하늘 반사 대비 블렌드 비율
- `thickness: 0.1` — *"Depth-ratio threshold that rejects false reflections from
  geometry close in front of the water"* (우리가 굴절에서 쓴 깊이 거부와 같은 종류)
- `maxDistance` / `stepCount` — 품질 티어가 정하고, 티어를 바꾸면 수동값이 초기화됨
- **high/ultra 전용**, 수중에서는 자동 비활성

- **G6** — 카메라를 따라가는 700 m 위 512² 격자의 감쇠 파동방정식.
  물체가 자기 흘수를 주입하고, 포말은 `|∇h|`에서 파생된다.
  **비분산 방정식은 켈빈 각을 만들 수 없다** — `asin(c/v)` 마하 원뿔이고 속도에 따라
  변한다. 켈빈 19.47°가 일정한 *이유*가 심해의 분산성(`ω=√(gk)`)이기 때문이다.
  그래서 필드가 교란을 맡고 해석적 팔이 각도를 유지한다. 파일에 명시.
  비용(4K 페어드): 델타 −0.65 / 1.35 / 1.28 → 중앙값 **1.28 ms**.
  한 라운드가 음수라 신호가 잡음 바닥 근처다 — 약 1.3 ms ± 1로 읽을 것.

### 물이 "인공적"으로 보이던 마지막 구조적 이유

바다가 전경부터 수평선까지 하나의 평평한 파랑이었다. Fresnel은 문제가 아니었다 —
근거리 약 14 %, 중거리 64 %로 정확히 기울어진다. 그런데 **기울기는 양 끝이 달라야
보인다.** 밝은 파랑 부피를 옅은 파랑 하늘에 섞으면 아무 일도 안 일어난다.

실제 심해는 거의 검다. 그 어둠이 Fresnel 램프를 "가까이 어둡고 멀리 은빛"으로
만들고, 그게 사진과 렌더를 가르는 것의 대부분이다.

부피가 밝았던 이유: 상향 산란항이 `waterScatter × skyLight × 0.80`이라는 **상수**였고,
그것이 떠나오는 물과 아무 연결이 없었다. 두 줄 위에서 `albedo = bb/(a+bb)` —
말 그대로 들어가서 되나오는 빛의 비율 — 를 계산해놓고 **크기를 나눠 버리고 색상만**
쓰고 있었다. 이번 프로젝트가 찾은 다른 결함들과 같은 모양이다: 물리량을 계산한 뒤
정작 중요한 부분을 정규화로 지웠다.

**남은 것**: G5(SSR).

## 4. 권고 순서

1. **G7** — 몇 줄. 이미 있는 항을 한 번 더 곱한다.
2. **G1 + G2** — 함께 해야 한다. 이 프로젝트에서 **단일 최대 시각 개선**이고,
   구조 변경이지만 재료(`pass(scene, camera)`)가 이미 있다.
3. **G4** — 채널 하나. 싸고 확실하다.
4. **G3** — 새 서브시스템. 움직임에서의 인상을 크게 바꾼다.
5. **G6**, **G5**, **G8** — 그 다음.

## 5. 하지 않을 것

- 참조의 프리셋 이름, UI 배치, 카피, 로고를 따라가지 않는다.
  (게임 타이틀명을 쓰지 않은 기존 결정과 같은 이유.)
- 참조 스크린샷을 자산으로 쓰지 않는다.
- 위 기법은 전부 공개된 일반 기법이며, 구현은 우리 코드베이스에서
  독자적으로 작성한다. 오픈소스를 직접 가져오는 경우 `research.md` 규칙대로
  파일별 라이선스와 고지를 기록한다.
