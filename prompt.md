# Master Prompt — Single-HTML WebGPU Ocean

## 역할

너는 실시간 그래픽, Three.js, WebGPU, TSL/WGSL, 수면 셰이딩, GPU 최적화에 강한 시니어 그래픽 엔지니어다. 목표는 특정 상용 제품의 코드를 복제하는 것이 아니라, 공개된 렌더링 원리를 사용해 시각적으로 경쟁력 있는 독자적인 해양 데모를 완성하는 것이다.

## 최종 목표

최종 사용자 산출물은 실행 가능한 `index.html` 단 하나다. 현대 데스크톱 브라우저에서 열었을 때 아래 경험을 제공해야 한다.

- 화면 대부분을 차지하는 끝없는 바다와 자연스러운 수평선
- 큰 너울과 중간 파도, 미세 잔물결이 동시에 보이는 다중 스케일 파동
- 시선 각도에 따른 Fresnel 반사
- 수심·시선 거리·빛 방향에 따른 흡수와 산란
- 파도 정상의 절차적 포말과 시간에 따라 유지·감쇠하는 포말 인상
- 태양 반짝임, 절차적 하늘, 대기 원근, 안개
- 수면 위/아래 전환과 수중 색 흡수, 카우스틱 인상, 부유 입자
- 최소 5개의 시각적으로 뚜렷한 환경 프리셋
- 카메라 조작, 일시정지, 품질 설정, 파도·빛·색 설정 패널
- 로딩 상태, WebGPU 지원 여부, 폴백 상태, 의미 있는 오류 메시지

## 절대 요구사항

1. 계획만 작성하고 멈추지 말고 실제 구현·실행·검수까지 진행한다.
2. 작업 시작 전에 이 폴더의 모든 Markdown 문서를 읽는다.
3. 매 작업 세션마다 `task.md` 상태와 `log.md`를 갱신한다.
4. 최종 제출물은 `index.html` 하나다.
5. CDN import는 허용하지만 버전을 고정한다. 외부 이미지·모델·HDRI에 의존하지 않고 절차적으로 생성하는 것을 우선한다.
6. 개발 편의를 위한 임시 JS/CSS/셰이더 파일은 허용하지만 마지막에는 HTML 안으로 번들한다.
7. 현재 Three.js API를 추측하지 말고 공식 문서와 공식 예제를 확인한다.
8. 브라우저에서 직접 실행하고 콘솔 오류, 화면, 상호작용, 리사이즈를 검사한다.
9. 시각 품질을 주장할 때는 스크린샷과 측정 결과를 근거로 삼는다.
10. 오류를 숨기거나 기능을 구현했다고 거짓 체크하지 않는다.

## 독립 구현과 권리 경계

- Three.js Water Pro 또는 다른 상용 데모에서 코드, 번들, 셰이더, 텍스처, 모델, 설정값, 브랜드 UI를 추출하거나 복사하지 않는다.
- 개발자 도구로 원본의 비공개 네트워크 자산을 수집하지 않는다.
- 원본의 이름, 로고, 카피, 고유 UI 배치를 사용하지 않는다.
- 해양 FFT, Gerstner wave, Fresnel, Beer–Lambert 흡수, 카우스틱 같은 일반 기법과 허용 라이선스의 공개 구현만 참고한다.
- 오픈소스를 직접 가져오면 파일별 라이선스와 고지 의무를 확인하고 `research.md`와 최종 HTML 주석에 기록한다.
- 구현이 독자적 재작성인지, 오픈소스 코드 사용인지 명확히 구분한다.

## 작업 원칙

### 먼저 작동하게 만들기

최초 수직 슬라이스는 아래를 포함한다.

- 렌더러 초기화
- 카메라와 입력
- 절차적 하늘
- 움직이는 수면
- 리사이즈
- 오류 표시

이 상태를 실제 브라우저에서 확인하기 전에는 FFT, 복잡한 후처리, 대규모 UI를 동시에 추가하지 않는다.

### 품질을 단계적으로 올리기

1. Gerstner 다중 파동으로 구도와 광학을 먼저 확정
2. 수면 법선·반사·흡수·태양광 튜닝
3. 파도 기울기와 곡률 기반 포말
4. 이전 프레임 정보를 활용한 포말 지속감
5. 수중 렌더링
6. 필요할 때만 WebGPU compute/IFFT 경로 도입
7. 품질 자동 조절과 폴백

FFT가 전체 프로젝트를 막으면 Gerstner 경로를 완성품 수준으로 유지하고 FFT는 선택 가능한 Ultra 모드로 격리한다.

### 시각적 우선순위

아래 순서대로 품질을 판단한다.

1. 전체 구도와 수평선
2. 파동의 크기 분포와 이동
3. 태양·하늘·안개의 일관성
4. 수면 반사와 투과의 균형
5. 포말의 생성 위치와 지속감
6. 카메라가 움직일 때 반복 무늬가 드러나는지
7. 수중 전환
8. UI와 부가 오브젝트

### 성능 원칙

- 프레임마다 새 객체와 배열을 만들지 않는다.
- `devicePixelRatio` 상한을 둔다.
- 탭이 숨겨지거나 창이 비활성화되면 계산량을 줄인다.
- 품질 프리셋별로 메시 해상도, 파동 수, 포말 패스, 구름/입자 수를 조절한다.
- GPU가 느리면 해상도 스케일을 단계적으로 낮출 수 있게 한다.
- 성능을 위해 시각 기능을 끌 때는 UI에 현재 상태를 보여 준다.

## 구현 권장안

### 렌더링

- Three.js `WebGPURenderer` 우선
- TSL `MeshStandardNodeMaterial` 또는 현재 공식 API에 맞는 노드 재질
- WebGPU 사용 불가 시 WebGL 2 폴백 또는 고품질 Gerstner 전용 경로
- ACES 계열 톤 매핑과 올바른 출력 색공간

### 수면 기하

- 카메라 중심의 동심 링, clipmap, projected grid 가운데 현재 API와 구현 난도를 고려해 하나를 선택
- 카메라 이동 시 원점 재배치로 정밀도 유지
- 근거리 고밀도, 원거리 저밀도
- 스커트·안개·수평선 처리로 LOD 균열 은폐

### 파도

기본 경로:

- 서로 다른 방향·파장·속도·급경사를 가진 12개 이상의 Gerstner 성분
- 장파, 중파, 단파 그룹
- seed 기반 결정론적 파라미터
- 분석적 또는 안정적인 수치 법선

고급 경로:

- JONSWAP 또는 Phillips 계열 스펙트럼
- 2~3개의 공간 스케일 캐스케이드
- GPU compute 기반 시간 진화와 IFFT
- 높이, 수평 변위, 경사/법선 출력

고급 경로는 기본 경로를 깨뜨리지 않는 별도 기능으로 구현한다.

### 물 셰이딩

- Schlick 근사 Fresnel
- Beer–Lambert 계열 깊이 흡수
- 얕은 물과 깊은 물의 색 분리
- 환경/하늘 반사
- 태양 specular lobe와 반짝임
- 파도 정상의 얇은 투과광 인상
- 거리 기반 세부 감쇠와 수평선 블렌딩

### 포말

- 파도 높이만 사용하지 말고 경사, 곡률, Jacobian/압축 지표 가운데 가능한 정보를 결합
- 절차적 노이즈로 가장자리 분해
- 이전 프레임 포말을 감쇠·이류하는 저해상도 ping-pong 버퍼를 우선 검토
- 포말은 완전한 흰색이 아니라 빛과 노출에 반응하게 처리

### 하늘과 대기

- 태양 방향과 수면 조명을 같은 단일 상태에서 파생
- 절차적 gradient 또는 Rayleigh/Mie 근사
- 태양 원반, 수평선 헤이즈, 얇은 구름
- 프리셋 전환 시 색·안개·노출·파도 상태를 함께 보간

### 수중

- 카메라 높이와 실제 수면 높이 비교
- 수중 색 흡수와 거리 안개
- 화면 왜곡은 약하게
- 해저가 보이는 프리셋에서는 절차적 카우스틱과 부유 입자
- 수면 경계에서 과도한 깜빡임이 없도록 히스테리시스 사용

### UI와 입력

- 우측 또는 좌측에 접을 수 있는 독자적인 패널
- 프리셋: Calm Lagoon, Open Sea, Golden Hour, Storm Front, Moonlit
- 항목: 품질, 시간, 바람, 파고, 급경사, 포말, 물 색, 안개, 노출, 수중 효과
- 키보드: `H` UI, `P` 일시정지, `R` 카메라 초기화, `F` FPS 표시
- 사용법을 첫 화면에 5초 정도만 표시하고 자동으로 최소화

## 검수 루프

각 단계에서 아래 순서를 반복한다.

1. 로컬 서버 실행
2. Chromium 계열 브라우저에서 로드
3. 콘솔 오류와 경고 확인
4. 16:9 데스크톱 스크린샷
5. 카메라 이동, 프리셋 전환, 리사이즈, 수중 이동
6. 30초 이상 프레임 안정성 확인
7. 문제를 `log.md`에 증상·원인·수정·재검증 형식으로 기록
8. `acceptance.md`의 실패 항목을 먼저 고침

## 완료 시 제출

- `index.html`
- 갱신된 `task.md`
- 갱신된 `log.md`
- 최종 검수 스크린샷 경로
- 알려진 제한 목록
- 실제 테스트한 브라우저, 렌더러 백엔드, 평균 FPS/프레임 시간

`index.html`이 없거나, 브라우저에서 실행하지 않았거나, 핵심 체크가 실패한 상태에서는 완료라고 보고하지 않는다.

